# 🚀 QManager RM520N BETA v0.1.14-draft

Custom SIM Profiles gets a full redesign and absorbs Connection Scenarios, so one page now opens by telling you exactly what your modem is running right now — the profile in force, the scenario it's bound to, and a 24-hour strip of when that scenario changes today — with a guided step-by-step wizard for creating profiles and ready-made suggestions for eight carriers. QManager also remembers your SIM cards properly now: a new **Tracked SIMs** list in System Settings shows every SIM the modem has seen, and the "New SIM card detected" banner finally stays dismissed for good, per SIM, on the device itself. The dashboard's **Speed Test** has been rebuilt too, with a live stepper, a full result panel, and a tile that picks up a test you started anywhere else. And **Cellular and Radio Information** has been redesigned from the ground up: every band you're on shows its own signal readings side by side with no clicking, and the page finally tells you when its numbers have stopped updating. **SMS Center** has had the same treatment — separate storage meters for your modem and your SIM, an inbox that stays on screen when the modem is busy, and honest progress when you clear everything out. And all three **Cell Scanner** pages have been rebuilt so a three-minute full sweep and a two-second neighbour read finally look like the very different things they are. **Cellular Basic Settings** rounds it off: every setting now tells you what it will do before you change it, changes are staged and applied together instead of one at a time, and the modem can finally be told to notice a SIM swapped while it's running. The four pages beside it — **APN Management**, **Network Priority**, **IMEI Settings** and **Blocked Networks** — have been rebuilt to match, and all four now speak every language QManager does. And a brand-new **Traffic Engine** under **Local Network** brings back DPI bypass for carriers that throttle video: **Video Optimizer** desyncs just the sites you list, **Traffic Masquerade** desyncs everything at once, and a **Test bypass** button measures before-and-after speed in one click.

> One-click OTA from **System Settings → Software Update** if you're on v0.1.5 or newer.

## ✨ New Features

- **Beat site throttling with one switch.** A new **Traffic Engine** page under **Local Network** — **Video Optimizer** garbles the connection handshake for just the sites on your list (speedtest.net, netflix.com, youtube.com and fast.com are pre-filled), **Traffic Masquerade** does it for every site at once, and the two modes take turns so they can't fight each other. The engine itself is downloaded on-device straight from the zapret project and verified against its official checksums before it ever runs (works without any PC in the middle).
- **Know whether the bypass is helping, in one click.** The **Test bypass** button runs a real two-phase speed test — without the engine, then with it — and shows both numbers and the improvement factor. The test turns the engine on and off by itself and always restores whatever you had before.
- **Edit the list of protected sites.** Video Optimizer's hostlist is editable in the page, and a save applies instantly — the engine re-reads the list on every new connection, no restart (subdomains match automatically).

- **See exactly how your bands stack up, on one strip.** The dashboard's Secondary Carriers card is replaced by a full-width **Carrier Aggregation** strip: a proportional bar where each band's width is its real bandwidth, plus a tile per carrier with its role, PCI, ARFCN, and signal meter. On 5G NSA the 5G leg is labelled **NR Anchor** instead of "secondary", since it is the carrier doing most of the work (role is derived in the interface only; the modem's own reporting is untouched).
- **A dropped band no longer just disappears.** If a carrier is released, its segment stays in place greyed with a "Released 3 min ago" note before fading out, so you can see what you lost instead of watching the strip silently redraw (a 6 second grace period keeps a momentarily missed radio read from flickering the whole strip).
- **Ready-made profiles for your carrier, right in your profile list.** Insert a SIM and **Cellular → Custom SIM Profiles** adds ready-made setups — APN and TTL already filled in — straight into your **Saved Profiles** list, marked **Suggested** and sitting below the profiles you actually saved. Recipes cover T-Mobile, T-Mobile Home Internet, Verizon, AT&T, Smart, Globe, GOMO, and DITO. Each one shows exactly what it will configure before you press Create, disappears once you have a profile for that SIM, and comes back if you delete it (the T-Mobile pair also includes a 5G band lock, checked against what your modem actually supports before anything is applied; the other carriers set APN and TTL only, so your Band Locking page stays available).
- **Prepaid and reseller SIMs are left alone.** SIMs from resellers that ride a bigger carrier's network — Mint, Google Fi, US Mobile, Visible, Cricket, Metro and others — no longer get their host carrier's suggestions, since those need a different APN. The check reads the service-provider name written on the SIM itself, which is the only place a reseller's identity actually appears.

- **See every SIM QManager remembers.** A new **Tracked SIMs** card in **System Settings** lists each SIM by carrier, phone number, and the date it was first seen, marks the one currently in the modem, and shows whether it still raises a new-SIM alert. SIMs already known to your device appear on the list after updating, with alerts already switched off (existing SIMs are backfilled from the known-SIMs list; historical entries show "Added before tracking began" rather than a made-up date).

- **QManager now tells you when your signal won't sit still.** If the modem keeps hopping between bands or cells, Recent Activities raises a single amber **"LTE band unstable — 14 changes in the last 5 min"** instead of one row per hop, so a genuinely flapping connection stands out rather than hiding in the noise (six changes inside five minutes trips it, and it's rate-limited to one warning per window).

- **The Speed Test has been rebuilt, start to finish.** Open it from the dashboard's Live Latency card and every stage now has a proper screen — a Latency → Download → Upload stepper that fills in as each measurement lands, big live readouts in the colour of whatever is being measured, and a full result panel with jitter, packet loss and the server you hit. If a run fails partway it now says *which* step died and keeps whatever it already measured, instead of throwing the whole thing away.
- **A test you started somewhere else now shows up on the dashboard.** Start a speed test in another tab — or from another device on your network — and the Live Latency card's Speed Test tile picks it up on its own, showing the live phase and reading without you opening anything (the tile polls a light 10-second heartbeat and speeds up only while a run is actually in flight).

- **Every band you're on, with its own signal readings — no clicking.** **Cellular → Cellular and Radio Information** shows one row per carrier with that band's RSRP, RSRQ, SINR and RSSI and their meters all on screen at once, in fixed columns so a weak leg lines up under the healthy ones. Frequencies, duplex and 5G subcarrier spacing sit behind a single **Show band reference** button that opens them on every row together. A value the modem didn't report says so instead of showing an empty bar, and anything the page worked out for itself rather than read from the radio is marked **Derived**.
- **The page now says whether you're looking at live numbers.** A **Live** chip beside the title pulses while readings are arriving and switches to **Data is stale** the moment they stop, so a frozen page no longer looks identical to a working one. The spectrum and connection halves are stacked full-width instead of squeezed into two columns, and **Link uptime**, the address family actually carrying your traffic, and an estimated **Distance to site** join the connection details (the distance is worked out from the network's timing-advance value, so it's marked **Estimated**).
- **Copy your radio diagnostics in one tap.** A **Copy diagnostics** button on the same page puts a tidy text summary of your network type, operator, aggregate bandwidth and every carrier's readings on the clipboard, ready to paste into a support thread. It deliberately contains no IMEI, ICCID, IP addresses, DNS, cell IDs or APN, so it's safe to post publicly without checking it first.
- **Turn the new-SIM alert back on for any SIM.** If you dismissed the "New SIM card detected" banner and want it back — say you're testing a profile — hit **Show Alert** on that SIM's row in Tracked SIMs and the banner returns, no reboot or reload needed.

- **Antenna Statistics has been rebuilt.** **Cellular → Antenna Statistics** now opens with a strip showing your MIMO layers, serving mode and how many of your four chains are reporting, above one card per radio holding all four ports with RSRP, RSRQ and SINR meters and a quality verdict on each. Every port keeps its place whether or not it's reporting, so a dead or unplugged antenna is obvious instead of quietly vanishing from the list.

- **Antenna Alignment now gives you one number to aim by.** **Cellular → Antenna Alignment** opens with a live **Live Aim** score out of 100, alongside the two readings behind it, their weights, your best score so far this session, and how much it just moved — so you can rotate an antenna and watch for improvement instead of reading four columns of dBm. The position recorder below it speaks the same score, so a live 74 and a recorded 78 mean exactly what you'd expect.

- **Your profiles and scenarios are one page now, and it opens with what's actually running.** **Cellular → Custom SIM Profiles** leads with an **in force now** card — the active profile, its APN and TTL, the connection scenario it's bound to, and the radio settings that scenario owns — with your saved profiles and every scenario side by side underneath. Connection Scenarios is no longer a separate sidebar item; old links to it land on the right place automatically.
- **See your whole day's scenario schedule at a glance.** If your active profile switches scenarios on a schedule, a full-width card under the "in force now" card draws the whole day as a 24-hour strip with a marker on the current time, so "Gaming takes over at 18:00" is something you can see rather than work out (each profile row carries a miniature of the same strip, and both read from one shared calculation so they can't disagree).

- **You can now tell QManager how much animation you want.** A new **Animations** row in the sidebar account menu, beside the theme picker, offers **System**, **Full** and **Reduced** — so you can quiet the interface down without changing an OS setting, or keep the animations even if your system asks for less (the choice is stored on your device and applied before the page paints, so nothing flashes on the way in).

- **Your modem can now notice a SIM swap while it's running.** A new **SIM Hot-Swap Detection** switch under **Cellular → Cellular Basic Settings** lets the modem spot a card being inserted or removed without a reboot — previously a swapped SIM went unnoticed until the next restart (it also reports whether a card is actually present, which now shows on the same page).

- **See what's in both SIM slots at a glance.** **Cellular → Cellular Basic Settings** now shows a **SIM slots** line at the top of the page listing each physical slot, which one the modem is using, and the last four digits of the card in it — so you can tell whether your second slot is empty without pulling the modem apart (read live from the modem; only the last four digits are ever shown, and the line is simply left out on firmware that can't report it).

- **See what your carrier actually handed you, not just what you asked for.** **Cellular → APN Settings** now ends with a **What the network granted** strip showing the APN you're really attached on, whether a bearer is up, and your IPv4 and IPv6 addresses — so a setting that saved but didn't take is visible on the same page (read live from the modem's own reporting, separately from your stored settings).
- **"Reboot Later" on the IMEI page now actually remembers.** Write a new IMEI, choose Reboot Later, and the page keeps an amber reminder at the top with **Review** and **Reboot** buttons until you restart — previously the choice was thrown away and nothing told you the modem was still on its old identity (the reminder lasts for the browser session and clears itself once you reboot).

## 🛠️ Improvements

- **The welcome screen now leads with whether it's working.** The page you land on before signing in has been rebuilt to match the rest of QManager: **Internet** and **Temperature** sit at the top, your carriers underneath, and the network details last — so the first thing you read is the answer to the question you opened it for. Temperature also speaks when things are fine now, rather than only when the modem is running hot, and a modem that isn't reporting a temperature at all no longer looks the same as a healthy one.
- **The status tiles are readable without relying on colour.** Each one now carries its own icon on a coloured disc against a plain tile, instead of tinting the whole tile a pastel shade — the old "healthy" and "warning" fills were nearly identical to the eye and completely identical to anyone with red-green colour blindness. The sign-in screen's heading was resized to match the welcome screen's, so the two finally look like the same product.

- **The band sweep page no longer jumps around when you start a scan.** **Cellular → Cell Scanner** now opens as a compact launch bar instead of a half-empty panel, and grows into the progress view as the sweep starts rather than two panels resizing at once. The duplicate "start" button is gone — there's one place to begin a sweep, not two.
- **Sweep results now show signal quality at a glance.** Each operator in the summary carries a coloured dial showing its strongest cell — green, amber or red, each with its own icon so the difference is readable without relying on colour. Previously the tiles were tinted by list position, which looked meaningful but wasn't.
- **Cellular Basic Settings has been redesigned, and now says what each setting will do.** **Cellular → Cellular Basic Settings** is one clean list of settings rows, each with a plain-English line underneath telling you the consequence — "Switching slots drops the connection for about 15 seconds" — instead of six unlabelled dropdowns. Choices with three or four options are now tappable pill groups rather than menus you have to open.
- **Changes are staged and applied together.** Edit as many settings as you like and a **"3 changes pending"** bar appears with a Save and a Discard; each edited row highlights and shows its own **before → after**, so nothing reaches your modem until you say so.
- **A slot change no longer looks like a hang.** Switching SIM slots takes the modem about 35 seconds, and the save bar now names the stage it's on — writing, waiting for the radio to settle, confirming what applied — instead of spinning silently. If some settings apply and others don't, the ones that failed stay pending so you can retry just those.
- **The page now shows what your modem actually reports.** A **What the modem reports now** card sits beside the settings with your carrier, active slot, SIM readiness, and serving technology — so you can see the effect of a change without leaving the page.
- **Cellular Basic Settings now speaks all five languages.** The whole page was previously hardcoded English; every label, option, consequence line and status is now translated into English, Simplified Chinese (简体中文), Traditional Chinese (繁體中文), Italian (Italiano), and Indonesian (Bahasa Indonesia).
- **The apply steps now tell you what they actually did.** Activating a profile shows a **Details** list where every step carries its own result — the APN that landed, "already set" for a step that was skipped, or the exact modem error for one that failed — instead of just a tick. If a step fails, the button offers **Reapply profile** and says plainly that the whole sequence runs again, because the modem has no way to retry a single step.
- **Deactivating a profile now puts your APN back to the carrier default.** Switching off the active profile under **Cellular → Custom SIM Profiles** used to leave the modem on that profile's APN and simply remove the "Active" badge — so deactivating didn't actually undo what applying did. It now reverts properly, and the confirmation dialog says so instead of promising nothing will change (expect the same brief cellular drop as applying; your IMEI and TTL settings are still left alone).
- **Creating a profile no longer takes up half the page.** The four-step wizard moved into a dialog opened by **New profile**, so the page stays focused on what your modem is doing rather than on a form you're usually not filling in.
- **Dialogs now sit properly above the page, everywhere in QManager.** Every dialog, alert and slide-out panel was painted the same shade as the page background, so in dark mode it actually looked *lower* than the cards behind it; they all now sit on the same raised surface as a card, with the extra outline dropped since the shade alone does the separating. The card showing your active profile also fades between "in force" and "nothing active" instead of snapping.
- **The sign-in and welcome screens have been redesigned.** The public status page at `/` and the login screen now use the same tonal look as the rest of the refresh — filled band pills, rounded signal rows, and clearer notices instead of faint tinted boxes (both screens share one type scale and one icon set, and now carry a theme toggle and language picker side by side).
- **Mistyping your password no longer costs you five minutes.** Log in wrong once too often and you're locked for 30 seconds — then 2, 5 and 15 minutes only if the wrong guesses keep coming, so an owner who fumbles is straight back in while a script gets buried. The form also tells you how many tries are left before it locks, and the countdown now survives a page refresh.
- **The Ethernet Status page has been redesigned.** **Local Network → Ethernet Status** now opens with colour-coded summary tiles for link state, speed, duplex and negotiation instead of a wiring diagram, and the speed limit keeps its instant apply with an **Applying… / Saved** confirmation right in the control. The page now also speaks all five languages.
- **The LTE and 5G cards on the dashboard got a refresh.** Signal readings now sit on rounded tinted rows with a filled quality chip, so "Excellent" or "Poor" reads at a glance instead of hiding in the colour of a number, and the band name gets its own coloured pill (part of the ongoing move to the new tonal design language).
- **You can tell the LTE and 5G cards apart from across the room.** The quality chip on each dashboard signal card is now coloured by radio — blue for 5G, violet for 4G — while signal quality itself is shown by the number of bars in the chip's icon, so quality still reads in greyscale, in sunlight, and to colour-blind eyes.
- **Every dashboard reading now says its quality out loud to a screen reader.** Values like SINR and RSRP add a hidden "Good" or "Fair" word alongside the colour, because green and amber sit at almost identical brightness and merge completely under the most common form of colour blindness.
- **A dropped connection now looks dropped.** The Network Status orb's failed state has a proper red ring instead of a grey one with a single red tint, and it stays still while a healthy connection pulses — so colour tells you how bad it is and motion tells you whether anything is alive.
- **One icon style across the whole dashboard.** The dashboard was drawing from four different icon sets at once; every card now uses the same rounded set as the sidebar, with bigger, clearer glyphs in the status orbs (the bundled icon font grew by about 9 KB and is still served entirely from the modem, so nothing is fetched from the internet).
- **The entire Cellular section now matches, too.** Cell Scanner, Band Locking, SMS Center, APN Management, Antenna Alignment, Tower Locking, Custom SIM Profiles and every other page under **Cellular** now use the same rounded icon set as the Cellular and Radio Information page they sit beside, instead of switching styles the moment you tap into one (the bundled icon font grew by about 9 KB to cover it, still served entirely from the modem).
- **Custom SIM Profiles has a whole new look.** Creating a profile is now a guided four-tab wizard — Identity, Network, Scenario, Review — with Load-from-SIM quick-fill, a "use my saved APN" pick, a live duplicate-SIM warning, and a summary step before you save. Saved profiles now show as a list of rows — carrier, APN, scenario and bands at a glance, a ring around the active one, and an "Applied/Partial/Failed at HH:MM" note — instead of a plain table. Find it under **Cellular → Custom SIM Profiles**, fully translated in all five languages (frontend-only redesign ported from the RM551E build — the profile data model and 4-step apply pipeline are unchanged; Verizon-specific UI is intentionally omitted since it doesn't apply to this modem).
- **Custom SIM Profiles now reads as one page, not two bolted together.** Profile rows, scenario tiles, the schedule strip and the apply dialog under **Cellular → Custom SIM Profiles** all share one set of shapes and one way of showing status, so a saved profile and a scenario look like things on the same page. Rows and tiles are plain now, with the colour on the small round icon and a labelled badge saying the state in words instead of a wash across the whole box — which also means status still reads in greyscale and to colour-blind eyes.
- **Your APN, TTL, hop limit and IMEI override are visible at a glance again.** The "in force now" card lists all of them on its Identity tile, and any profile with an **IMEI override** now says so on its own row in Saved Profiles — that setting changes the identity your network sees and was previously invisible unless you opened the profile.
- **A part-applied profile now offers Reapply right where it says something failed.** If some steps land and others don't, the warning on the "in force now" card carries its own **Reapply profile** button — previously you had to close the dialog and hunt for the row's menu to try again (the modem can only re-run the whole four-step sequence, so that is what it does).
- **"No profile is active" now offers to activate one.** If you have saved profiles and none is in force, the card offers **Activate a profile** and takes you to your saved list, instead of offering a second **New profile** button identical to the one at the top of the page. It moves you there rather than picking one for you — activating still happens on the profile you choose. The card also stops changing size as it swaps between states.
- **The empty profiles card finally lets you create one.** With no profiles and no suggestions for your SIM, **Saved Profiles** offered only **Refresh** — a button that reloads a list you already know is empty. It now shows **New Profile** underneath, next to the explanation of what a profile actually does.
- **"Edit schedule" opens on the schedule.** The Edit schedule button on the 24-hour strip now drops you straight onto the wizard's **Scenario** step instead of making you click forward through three tabs to reach the thing you asked for.
- **Connection Scenarios now speaks all five languages.** The scenario picker, tiles, and active-config card under **Cellular → Custom SIM Profiles** are fully translated into English, Simplified Chinese (简体中文), Traditional Chinese (繁體中文), Italian (Italiano), and Indonesian (Bahasa Indonesia).
- **The new-profile form fills itself in from your SIM.** Open the create form under **Cellular → Custom SIM Profiles** and your SIM's APN and ICCID appear on their own, without pressing Load from SIM — and anything you've already typed is left alone if the read finishes while you're mid-sentence. IMEI is still only filled by pressing **Load from SIM**, on purpose: a stored IMEI reboots the modem when the profile is applied.
- **A clearer "New SIM card detected" banner.** The banner now names the SIM's carrier and phone number instead of just announcing a swap, and offers exactly one next step: **Apply Profile** when a saved profile matches the card, or **Create Profile** when none does — which now drops you straight into the profile creation form.
- **Dismissing the banner asks first, and says what it means.** Closing the banner now opens a short confirmation that spells out the SIM's ID and makes clear you're silencing the alert for *that SIM only* — every other card still raises it, and you can undo it from Tracked SIMs.
- **Remembered SIMs live with the list they belong to.** The SIM count and its **Clear** button moved off the System Settings card and onto the bottom of **Tracked SIMs**, right under the SIMs they act on — so System Settings is back to just display preferences, and Clear can no longer leave the list showing SIMs it claims to have forgotten.
- **Cellular and Radio Information has been redesigned.** The page under **Cellular** now opens with four summary tiles — network type, total bandwidth, carrier count and MIMO — above a cleaner cell-information card and the new per-band list, in the same tonal style as the rest of the refresh (part of the ongoing move to the new design language; the page reads the same data as before and makes no extra requests to the modem).
- **The radio page now tells you when it's registered and when it isn't.** No SIM, airplane mode, no service or still searching each get their own clear screen with a Retry button, instead of the normal layout filled with dashes. A **Live** indicator next to the title pulses while readings are arriving and turns into an amber **Stale** warning the moment they stop.
- **Carrier counts on the radio page are now counted, not inferred.** The number of active carriers is worked out from the bands the modem actually reports, which fixes 5G connections that could show one carrier fewer than they really had.
- **Cellular and Radio Information speaks all five languages.** The whole page, including the new band details and every state screen, is translated into English, Simplified Chinese (简体中文), Traditional Chinese (繁體中文), Italian (Italiano), and Indonesian (Bahasa Indonesia).
- **A friendlier "page not found."** Hit a broken bookmark or a mistyped link and you now land on a branded 404 that speaks QManager's language — an animated signal meter reaching for a lock it can't get, the exact address you asked for, and a one-tap **Return to Dashboard** — instead of a blank error page (new `not-found` route styled to match the login/splash screens; works in light and dark).
- **Antenna Alignment has been redesigned, in all five languages.** The page under **Cellular** now leads with the live aim instrument, then the three-position recorder, then a compact **Receive Chains** strip that links across to Antenna Statistics — translated into English, Simplified Chinese (简体中文), Traditional Chinese (繁體中文), Italian (Italiano), and Indonesian (Bahasa Indonesia). Clearing one recording or resetting all three now asks you to confirm first, and an idle antenna is stated plainly instead of being faded out.
- **Antenna Statistics now speaks all five languages.** The whole page — the context strip, every port block and both empty states — is translated into English, Simplified Chinese (简体中文), Traditional Chinese (繁體中文), Italian (Italiano), and Indonesian (Bahasa Indonesia). The breadcrumb above it follows your language now too, on both antenna pages, instead of always reading English.
- **SMS Center now speaks all five languages.** The inbox, compose dialog, and message views under **Cellular → SMS Center** are fully translated into English, Simplified Chinese (简体中文), Traditional Chinese (繁體中文), Italian (Italiano), and Indonesian (Bahasa Indonesia), matching the rest of the localized interface.
- **Smoother dashboard status changes.** The Network Status, Device Information and 4G/5G cards now ease between states instead of snapping — signal readings dip as they update, status chips fade between colours, and the signal meters glide to their new level instead of springing past it and settling back (motion retuned onto the shared 180/300/400ms token scale).
- **Dashboard readings now tick one after another instead of the whole card blinking.** Every live figure on the dashboard used to flash at the same instant on each poll, so the card flickered as one block; the numbers that actually changed now dip in reading order — top to bottom, or left to right on the Data Used and Carrier Aggregation rows (only the values that moved are staged, so there are no silent gaps and the cascade never outlives the poll that started it).
- **That tick is also slower and easier to follow.** The dip was quick enough to register as a flicker rather than something you could actually watch; it now takes its time on the way down and fades back at a steady rate instead of snapping most of the way home and creeping the rest (700ms end to end, staged 100ms apart, with a linear return so none of that time is spent below the threshold you can see).
- **Status chips finish their colour change properly.** Chips on Recent Activities, Live Latency and the 4G/5G cards now fade their label and icon together as the fill changes colour, instead of the icon snapping in a single frame or the words not moving at all — which matters because the icon is what tells the states apart if you can't rely on colour.
- **Recent Activities now highlights what's recent and what's still wrong.** The dashboard's activity list has been redesigned in the new tonal style, with a header chip reading **All clear** or a count of what's outstanding (each "Internet Lost" is paired with the "Internet Restored" that cancels it).
- **Recent Activities fades out old news, but never an open problem.** Rows on the dashboard's activity list stay highlighted for their first hour and then quiet down to a plain row with a coloured icon, except a problem that still hasn't recovered, which stays lit however old it is (routine band changes and cell handoffs get a neutral tint rather than a colour, so they never read as good or bad news). The list now shows the five most recent events instead of six.
- **Activity names now speak your language.** Every event label in the dashboard's Recent Activities card, from "Cell Handoff" to "Tower Lock Failover", is translated into all five languages, alongside the timestamps and the new status chip.
- **Your activity history goes back a lot further.** The dashboard keeps six times as much event history on the modem, so a busy few minutes on the radio can no longer push the outage you're trying to find off the end of the list (300 events retained instead of 50, with the same amount sent to your browser — no extra load).

- **The Speed Test now warns you what a run costs.** A line under the Run button tells you a test uses roughly 300–500 MB before you spend it — which matters on a metered SIM, and is easy to be surprised by since a *faster* connection actually burns more data (the test runs for a fixed time, not a fixed number of megabytes).
- **You can close the Speed Test while it's running.** The dialog used to silently ignore the X, Escape and clicking outside for the ~40 seconds a test takes; it now closes whenever you like, and reopening it re-attaches to the run still in progress.

- **SMS Center has been redesigned.** **Cellular → SMS Center** now opens with three summary tiles — unread count, modem memory, SIM memory — above a cleaner inbox in the same tonal style as the rest of the refresh, with the unread marker now a proper icon rather than a coloured dot that vanished in greyscale.
- **You can finally see how full your SIM is, separately from your modem.** The storage meters are now split: your modem holds 255 messages and your SIM 35, and they fill up independently. The old single "64/290" reading added two unrelated capacities together and made a nearly-full SIM look comfortable (needs this update on the device; older installs fall back to the combined meter automatically).
- **Clearing your inbox now shows what it's actually doing.** "Delete all" walks the two memories one at a time and reports each — so if the SIM fails and the modem succeeds, it says exactly that instead of one merged error.
- **SMS Forwarding's status card says what it means.** Waiting on a destination number is now shown in blue as a setup step rather than amber as a problem, and each of the four states — active, delivery issues, needs setup, off — has its own icon, so they still read apart in greyscale or with colour-blind eyes.
- **The SMS summary tiles are quieter, and the numbers stand out more.** The three tiles at the top of **Cellular → SMS Center** were each painted a different colour; they now sit on a plain surface with colour only on the icon, so your unread count is the first thing you see and the two storage meters read as the same kind of thing (the same treatment the Cellular and Radio Information tiles already got).
- **The message length warning is now visible without colour.** Composing a long text under **Cellular → SMS Center** used to signal "nearly at the limit" by tinting the counter and nothing else; it now adds a warning icon, and a second, different icon once you're over.

- **Save buttons across QManager got a proper confirmation.** Every Save, Apply and Update button now fades from its label to a spinner and then to a tick, without the button changing size or the row around it shifting — so you can see the save land instead of guessing (the three states share one slot, so the button is always as wide as its longest state, in any language).
- **Pressing Enter to save no longer loses your place.** Saving used to switch the button off for a moment, which knocked keyboard focus back to the top of the page; the button now stays focused for the whole save and simply ignores a second press.
- **Your settings survive a power cut during an update.** Config files rewritten by an update are now swapped into place in a single step instead of being rewritten in the open, so losing power mid-update can no longer leave one half-written on flash (also cleans up leftover scheduling entries from pre-timer versions, which a bug could previously skip entirely).

- **Band Locking has been redesigned.** **Cellular → Band Locking** now opens with a single status card at the top — your lock posture, the band-failover safety net and its state, and the bands actually on air — above one card per radio type instead of a status panel wedged into the same grid as the controls.
- **Bands are now tappable chips, and you can see a change before you apply it.** Each band is a chip you tap rather than a tiny checkbox: a filled chip is one you've selected, and a chip with a ring around it is one your modem is running right now — so a band you're about to add and one you're about to drop both read at a glance, with a pending-changes count beside the buttons. The old "unlock" button is now labelled **Restore all supported**, which is what it actually does.
- **Band Locking now speaks all five languages.** Every label, status, toast and screen-reader announcement on the page is translated into English, Simplified Chinese (简体中文), Traditional Chinese (繁體中文), Italian (Italiano), and Indonesian (Bahasa Indonesia) — previously the page was almost entirely hardcoded English.
- **The bands on air now show you how strong each one actually is.** Every carrier tile at the top of **Cellular → Band Locking** is a plain card with a coloured radio badge, and its signal reading carries the same five-step colour and bar the rest of QManager uses — the old fully-tinted tile had no room left to report signal quality at all, so it never did.
- **Band Locking has a Refresh button now.** The page only read your band configuration when it opened, so a scheduled scenario that changed bands on the modem left it showing stale numbers until you reloaded the browser; the header now has a **Refresh** that re-reads it in place without blanking the page (it stays greyed for the ~30 seconds after a lock, while the failover safety net is still checking the radio).
- **The band-failover switch got a row of its own.** It used to sit at the foot of the lock-posture list, where it read like a fourth radio setting and dropped to the very bottom of the page on a phone; it now spans the whole status card, which matches what it actually is — one safety net for the modem, not one per radio.
- **Tower Locking and Frequency Locking got matching page headers.** All three pages under **Cellular → Cell Locking** now share one header shape, so moving between them no longer feels like moving between three different products.
- **Tower Locking has been redesigned.** **Cellular → Cell Locking → Tower Locking** now opens with a single status card — the cells you're camped on right now, what each radio is actually locked to, and the failover safety net — above one card per radio and one for the schedule, instead of a status panel wedged into the same grid as the controls.
- **You can now lock onto a cell by tapping it.** Every cell on air is shown as a tile with its PCI, channel and signal, and a **Use this cell** button that fills in the form below — so you no longer have to copy numbers by hand. A cell you can't currently lock keeps its button, greyed, with a line saying why (5G SA locking needs the modem out of NSA mode, and LTE only has three slots).
- **Tower Locking now tells you when its numbers were last read.** The lock state carries an **as of 14:32** line and a refresh button, because reading it costs the modem three commands and so it isn't refreshed automatically — and a schedule, a failover, or another browser tab can change it behind your back.
- **A lock that won't survive a reboot now says so.** If the modem accepts your lock but the boot entry can't be written — or it rejects **Keep lock after reboot** outright — the page raises a dismissible amber notice instead of showing a plain success (both were already reported by the device and thrown away before reaching the screen).
- **"Keep lock after reboot" now reports the modem, not the setting file.** The chip reads the value back from the radio, and says **Split** if the 4G and 5G halves disagree — a real fault, since QManager writes one value to both.
- **Tower Locking now speaks all five languages.** Every label, status, dialog, toast and screen-reader announcement is translated into English, Simplified Chinese (简体中文), Traditional Chinese (繁體中文), Italian (Italiano), and Indonesian (Bahasa Indonesia) — previously the page was entirely hardcoded English.
- **The page now tells you when it can't read your lock state.** If the modem doesn't answer, Tower Locking says so and offers **Try again**, instead of drawing empty fields that look like a confident "nothing is locked".

- **Frequency Locking has been redesigned.** **Cellular → Cell Locking → Frequency Locking** now opens with a **Right now** strip — a plain-English verdict plus every carrier you're currently on — above one card per radio and a single bar at the foot, so all three Cell Locking pages finally read like one product.
- **Add a channel by tapping the carrier you're already using.** Every carrier on the new strip carries an **Add** button that drops its channel straight into the matching list, and a channel already in your list is ringed and marked — so you can see at a glance whether the radio is inside your own allow list. **Use current** now fills both LTE slots from the carriers actually on air instead of just the first one.
- **Every channel now shows the frequency it really lands on.** Type or add an EARFCN and the page prints its actual downlink frequency — EARFCN 1300 is **1815.0 MHz**, not the "1800 MHz" its band is marketed as — so a mistyped digit is visible before you apply it (worked out per channel, so it moves the moment a digit does).
- **The page now says out loud that nothing will rescue you.** Frequency Locking has no safety net: unlike Band Locking (~30 seconds) and Tower Locking (~80 seconds), nothing restores service on its own if the channels you allowed turn out to have no coverage. That's now stated on the verdict, in the bar, and in the confirmation dialog rather than left for you to find out.
- **You can stage up to 32 5G channels now, not four.** The 5G list was capped at four slots by a constant nobody meant as a limit; it's a proper list up to the modem's real ceiling now, with each entry carrying its own subcarrier spacing.
- **The bar at the foot shows what your modem answered, not what QManager sent.** Instead of echoing a command, it prints the lock the modem reads back alongside the channel you're actually serving on — `LTE 9485, 1350 · serving 9485` — which is the only thing that can reveal an entry the modem rejected, or a lock a tower lock quietly replaced.
- **A tower lock no longer greys out the whole page.** With one active, Frequency Locking explains that it won't write while the tower lock is held but keeps **Use current** working, so you can capture the channel you already validated before switching over — only Apply and Clear go dead.
- **The lock switches are gone from both cards.** Applying is a button behind a confirmation now, matching Tower Locking. The old switch both reported the modem's state *and* wrote whatever was sitting unsaved in the fields below it, so it could never tell you which of the two it was doing.
- **Frequency Locking now speaks all five languages.** Every label, status, dialog and toast is translated into English, Simplified Chinese (简体中文), Traditional Chinese (繁體中文), Italian (Italiano), and Indonesian (Bahasa Indonesia) — the page was previously entirely hardcoded English.

- **The Cell Scanner has been redesigned, and now tells you what a scan costs.** **Cellular → Cell Scanner** opens with a run panel that stays put while the scan runs — posture, elapsed time and result count in one place — above a results table that only ever shows rows. The button reads **Sweep all bands** and states plainly that a full sweep takes 30 seconds to 3 minutes and pauses everything else on your modem, where it used to say the same "Start New Scan" as the two-second neighbour read.
- **The neighbour scanner shows the readings it was already collecting.** **Cell Scanner → Neighbour Cell Scanner** now has RSRQ, RSSI and SINR columns — the whole point of a neighbour read, and previously available only by exporting the CSV and opening a spreadsheet. The search box now filters by channel instead of by an internal three-word label, and cell types read as plain English rather than `intra` / `inter` / `nr5g`.
- **A cell you can't lock now says why.** The lock button on a neighbour row used to vanish for 5G cells with no explanation; it's now shown greyed with a line saying that only LTE neighbours can be locked (a neighbour report carries neither the band nor the subcarrier spacing a 5G lock needs).
- **The Frequency Calculator matches the rest of QManager.** **Cell Scanner → Frequency Calculator** picks up the same page header, rounded cards and radio-coloured band pills as its two siblings, instead of looking like a page from an earlier version.
- **All three Cell Scanner pages now speak all five languages.** The scanner, the neighbour reader and the frequency calculator were entirely hardcoded English; every label, column, state and error is now translated into English, Simplified Chinese (简体中文), Traditional Chinese (繁體中文), Italian (Italiano), and Indonesian (Bahasa Indonesia).

- **APN Settings has been redesigned to match the rest of Cellular.** **Cellular → APN Settings** is now the same grouped-row layout as Cellular Basic Settings, with each choice explained underneath and the row you're editing highlighted until you save.
- **Picking a carrier profile is now a list you tap, not a dropdown.** The **Carrier Profile (MBN)** card is a switch for automatic selection plus a tappable list of the bundles your modem actually has, with the chosen one filled in — and it now works properly with a keyboard, one Tab in and arrow keys through the list, which matters on firmware that ships twenty-plus bundles.
- **Network Priority has been redesigned.** **Cellular → Network Priority** now shows each technology as a proper row with its rank, a line saying what that position means, and a **Serving now** chip on whichever radio is actually carrying your connection — and on 5G non-standalone it correctly marks both the 5G and 4G legs, since both are in use.
- **Reordering technologies now works with a keyboard.** The drag handles on Network Priority are real buttons you can Tab to and move with the arrow keys, instead of being pointer-only.
- **IMEI Settings has been redesigned, and the legal notice is now impossible to miss.** The warning about changing your IMEI used to be a tooltip on a small icon that you had to hover — and it was worded three different ways in three places. It's now a single notice sitting at the top of **Cellular → IMEI Settings**, always visible.
- **The IMEI page separates what your modem reports now from what you're about to write.** The old single field was sometimes a readout and sometimes a draft; they're now two clearly labelled rows, because a new IMEI only takes effect after a restart.
- **"FPLMN Settings" is now called "Blocked Networks".** The sidebar, the breadcrumb and the page title all said different things; they now agree, and the page explains what a blocked network actually is. Your existing bookmarks still work.
- **All four pages now speak all five languages.** APN Management's carrier-profile card, Network Priority, IMEI Settings and Blocked Networks were entirely hardcoded English; every label, state and message is now translated into English, Simplified Chinese (简体中文), Traditional Chinese (繁體中文), Italian (Italiano), and Indonesian (Bahasa Indonesia).

## 🐛 Fixes

- **Buttons work the instant a dialog closes.** Closing any dialog used to leave the page ignoring clicks for about a second — most noticeable on **Cellular → Custom SIM Profiles**, where you're opening and closing dialogs constantly — so a second tap simply did nothing. Dialogs, alerts and side panels now close in a third of the time and the page is live again as soon as they're gone (the underlying dialog library disables clicks on the whole page until the close animation finishes, and the backdrop was fading out well before that, which made the app look ready when it wasn't).
- **Recent Activities was only showing half of what happened.** The dashboard's activity feed logged what the background services did but silently threw away everything the web interface itself did — APN saves, profile changes, deactivations — so a change you just made never appeared. All events now land in the same feed (the log file was being created owned by the wrong user at boot, which locked the web interface out of it; it's now pre-created for both, and a dropped event says so in the log instead of reporting success).
- **Applying a SIM profile no longer shows you the last apply's progress.** The step-by-step dialog under **Cellular → Custom SIM Profiles** could sit on a stale result from a previous activation and never update, most often showing an old "complete" the moment you pressed Activate. Progress is now written where both the interface and the background worker can reach it (same file-ownership fix as above — the progress file was being replaced with one the web interface couldn't write).
- **Two profile activations can no longer run at once.** The lock that stops a second apply from starting while one is in flight was being destroyed on every run, so the web interface was effectively running unlocked; it's now kept intact for the life of the boot.
- **Closed a security hole that let the web interface take over the modem's background services.** A settings file that QManager's background services read as root was sitting in a folder the web interface can write to, so anything that got into the web interface could have taken full control of the device. It has moved to `/etc/qmanager.env`, out of reach — your update carries any existing log-level or ping overrides across automatically, but if you hand-edited `/etc/qmanager/environment` over SSH, that is the new path to edit from now on.
- **Your Discord bot token and email app password are no longer readable by the rest of the device.** Both were stored as plain text in a folder the web interface can write to, so any local account on the modem could read them; they now live in a root-only store, and this update moves them for you — there's nothing to re-enter. **Please rotate them anyway:** a permissions fix can't un-share a credential that was already exposed, so if you use Discord alerts, reset your token in the Discord developer portal (Application → Bot → Reset Token) and paste the new one into **Monitoring → Alerts**; if you use email alerts, generate a fresh Gmail app password and do the same.
- **Applying a SIM profile now really does change your APN.** Activating a profile under **Cellular → Custom SIM Profiles** could tick the APN step green while your modem carried on using the old APN — the step now re-attaches to the network and confirms what the carrier actually granted before reporting success (your APN only reaches the carrier at attach time, and the old check read back the modem's own stored setting, which always matched; expect a cellular drop of around ten seconds while a profile's APN is applied).
- **An APN save and a profile apply can no longer trip over each other.** If a profile activation is mid-way through re-attaching, saving on **Cellular → APN Settings** now says the modem is busy and asks you to try again in a moment, instead of both changes fighting over the connection.
- **An APN your carrier quietly refused no longer reports as saved.** Saving on **Cellular → APN Settings** now checks what the network actually granted and tells you when it differs from what you asked for, instead of reporting success the moment the modem accepted the setting locally (a carrier can hand back a different APN than the one requested — QManager now compares the two rather than reading back its own stored copy).
- **Your APN settings are actually remembered now.** **Cellular → APN Settings** would apply your APN to the modem but forget it the moment you left the page, and profile names on the slot list went the same way — the files behind both were being written to a folder the web interface isn't allowed to create files in, so every save silently did nothing. They've moved to a folder it owns, and your existing settings are carried over automatically on update.
- **Your IP address, gateway and DNS on the APN page are right again.** On an IPv6 or dual-stack connection the page could show a blank IPv6 address, no DNS servers at all, or your gateway listed as a DNS server. All three were the same misread of the modem's reply and are now handled correctly (the modem reports IPv6 as sixteen dotted numbers rather than colon-hex, and quotes the gateway only sometimes — both threw the old parser off).
- **The MTU page no longer reads the wrong connection.** **Network → MTU** always read from the modem's first data channel, but the live one moves between channels each time you re-attach — so it could report the MTU of a channel that isn't carrying anything. It now finds whichever channel your traffic is actually on.
- **Deactivating or deleting a SIM profile no longer returns an unreadable response.** Turning off a profile under **Cellular → Custom SIM Profiles**, deleting an active one, or switching SIM slots could fail in the interface even though the change went through on the modem — the reply contained two answers stuck together and the page couldn't read either (a schedule-teardown helper was printing its own result into the response body).
- **Editing the profile you're currently using now actually reaches your modem.** Saving changes to the active profile under **Cellular → Custom SIM Profiles** only rewrote the saved copy — your card kept showing the old settings, and a changed schedule left the modem still switching scenarios at the old time. Saving an active profile now re-applies it automatically, so the card, the modem and the schedule all agree (a rename-only save takes about three seconds and doesn't touch your connection; only a real APN change costs the usual brief cellular drop).
- **Deactivating a profile now shows you it's working.** Pressing Deactivate used to close the dialog instantly and leave you staring at an unchanged page for the eight to twelve seconds the modem actually takes. It now stays open with a progress state and finishes with a message telling you what happened — including a "modem was busy, try again" case that's completely safe to retry.
- **Custom SIM Profiles can no longer get stuck on loading placeholders.** If the modem hiccupped while the page was reading your active profile, **Cellular → Custom SIM Profiles** sat on grey placeholders forever — no error, no retry, and reloading didn't always help. It now tells you the details couldn't be read, says plainly that nothing on your modem has changed, and gives you a **Try again** button.
- **A profile activation started somewhere else now shows up here.** If an apply was kicked off in another tab, at boot, or before you navigated back, the page carried on as though nothing were happening — and if that run failed, your profile was switched off on the modem while the page still showed it as in force. The progress dialog now picks up a run already in flight and the page updates when it finishes.
- **Deactivate can no longer be pressed mid-apply.** The Deactivate button on the active profile row stayed clickable while another profile was being applied, which could push a second command into a busy modem. It — along with Reapply and Activate on every row — now goes dead until the modem is free.
- **The "in force now" card no longer reports someone else's progress.** Activating a second profile while one was still in force made the card show the *old* profile's name and details alongside the *new* one's "Applying" badge — and the warning underneath named the new profile, so one card claimed two different things at once. A part-applied warning could also stay stuck to the wrong profile until the next activation. The card now only ever reports the profile it is actually showing.
- **A long step message no longer breaks the apply dialog.** When activating a profile, a step reporting something wordy — like the full APN your carrier granted — used to stretch the dialog past its own edge and cut the text off mid-word, dragging the progress bar out with it. Everything now stays inside the panel and shortens with an ellipsis instead.

- **Fixed a reboot loop after upgrading with Scheduled Reboot enabled.** If you had **System Settings → Scheduled Operations → Scheduled Reboot** turned on, v0.1.13 could reboot the modem every 30–60 seconds whenever a SIM was installed — this update makes every QManager schedule ignore the one false trigger that fires while the modem is still setting its clock at boot (the modem has no clock battery; boots start at 1970 and systemd fires armed timers once when the network sets the real time).
- **A band lock applied by a SIM profile can no longer strand you offline.** If a profile's scenario locks bands your area can't serve, QManager now watches for a lost carrier and automatically reverts to all supported bands — the same safety net the manual Band Locking page has always had. This mattered most here, because binding that scenario also disables the Band Locking page, so there was no manual way back (opt-in via the existing band-failover setting; built-in Balanced/Gaming/Streaming scenarios are unaffected).
- **Recent Activities is no longer buried in band-change spam.** When your connection drops, the modem hunts through several bands before settling — and every step used to be logged, filling the entire dashboard activity list in about 12 minutes and pushing out the outage that caused it. QManager now waits for the band and cell to hold steady before logging anything, and reports it as **"LTE band settled on B28 (was B41)"** so it never claims a hop the radio didn't make.
- **A dropped signal no longer wipes out what the radio was doing.** Readings taken while you have no service used to overwrite the last good band and cell, which hid half of what happened and left messages like "5G NR anchor lost (was …)" with nothing after "was". Blank readings are now ignored instead of remembered.
- **"No Events" no longer means "we couldn't check."** If the dashboard fails to load your activity log, Recent Activities now says so and shows the reason, instead of displaying the reassuring empty state as though your network had simply been quiet (and if it already had events on screen, it keeps showing them with a notice above rather than blanking the card).
- **The radio page no longer shows frozen readings as though they were live.** If the modem stopped answering, **Cellular and Radio Information** kept displaying its last numbers with nothing to say so — and could announce every one of your bands as "released" when it was really just a missed read. It now flags stale data and holds the band list steady instead.
- **A band could look "released" without actually dropping.** The radio page and the dashboard's Carrier Aggregation strip both timed a carrier's release off *when the screen last redrew*, not off when the modem last reported it — so an unrelated screen update could occasionally nudge a still-active carrier over the release threshold. Both now time strictly off actual data arriving.
- **An idle antenna no longer reads as a failing one.** Ports your modem isn't using were being shown as a real "-140 dBm / Poor" reading with an empty bar, which looked like an antenna problem worth chasing; they now say **Not reporting**. Antenna Alignment was affected too, where an unused 5G chain could be listed as Active (the modem signals an idle port with values that sit exactly on the bottom of each scale — both pages now read them through one shared check).
- **Antenna Statistics no longer claims "no signal" when it can't reach the modem.** If the very first reading never arrived, the page used to show two "No LTE/5G Signal" panels — blaming the radio for a problem that was actually the connection to the device. It now says the modem is unreachable and offers a Retry, and flags stale readings the moment they stop updating.
- **Your recorded antenna positions can no longer re-rank themselves.** The recommended aim was scored using whichever radio the modem happened to be on at that moment, so a routine 4G/5G switch could quietly crown a different position while you weren't looking. Each recording is now scored purely from what it actually measured, so the winner only changes when you record something new.
- **"Averages 3 samples" now really means three.** The recorder counted every reply from the modem as a sample, but the modem only produces a fresh reading every ~4 seconds — so a recording often averaged the same measurement twice. It now waits for genuinely new readings, which takes about 8 seconds, and the card says so.
- **A recording is no longer punished for an idle antenna.** If one of the two readings behind a position's score was missing, that position lost a flat 40 points for a reason that had nothing to do with where you were pointing — occasionally handing the recommendation to a worse spot. The score is now reweighted across the readings that did report, and the slot marks itself as a partial result.
- **Antenna Alignment no longer claims "no antenna data" when it can't reach the modem.** If the very first reading never arrived, the page blamed the radio for what was really a lost connection to the device. It now says the modem is unreachable and offers a Retry — with a separate amber screen for the genuine case where the modem answers but no antenna is reporting, which is usually worth checking a cable for.
- **The 4G and 5G cards line up again in Italian.** A long heading could push one card's title onto a second line while the card beside it stayed at one, knocking the pair out of alignment; long headings are now trimmed instead of wrapping.
- **The profile form no longer repopulates itself after you edit a profile.** A SIM read that finished while you were editing could re-fill the form the next time you opened it; that stale response is now discarded instead of queued.
- **"Active" and "Not Active" scenario chips can now actually be told apart.** The Connection Scenarios detail card marked its status with nothing but a coloured dot, which read the same in greyscale or to colour-blind eyes; each status now carries its own icon.
- **Balanced, Gaming and Streaming show their own icons again.** All three built-in scenarios were drawing the same generic sparkle in the scenarios list — while the "in force now" card above showed the right one for the same scenario — so the list gave you nothing to tell them apart by (the built-in list existed twice in the code and the two copies had drifted; there is now one).
- **The "Band locking" link on the profile card no longer 404s.** The shortcut on the "in force now" card's Radio tile pointed at a page that doesn't exist, so tapping it dead-ended; it now opens **Cellular → Cell Locking**.
- **Scenario network modes, band locks and optimization labels are translated now.** "5G SA only", "Latency" and "Auto" stayed in English under **Cellular → Custom SIM Profiles** no matter which language you picked, because those words were baked into the code rather than the language packs. An optimization label you typed yourself is still shown exactly as you wrote it.
- **Cancel in the profile wizard now closes it instead of wiping your work.** When creating a profile, the button beside Save cleared all four tabs and sent you back to the start — the reflex reach for "get me out of here" deleted everything you had typed. It now just closes the dialog.
- **Your SMS inbox works again after an update.** Texts from both the SIM and the modem's own memory show up in **Cellular → SMS Center** again on devices that upgraded over-the-air, where the inbox could previously come up empty (in-app "Software Update" upgrades now refresh the bundled `sms_tool` binary they used to skip — the older stranded copy talked to the wrong device).
- **Clearing remembered SIMs now clears the list too.** Pressing **Clear** used to drop the count but leave every SIM sitting in **Tracked SIMs**; the count and the list now reset together, keeping only the SIM currently in the modem (they are two separate stores on the device and only one was being wiped — the SIM you're using keeps its dismissed-alert setting either way).
- **Dismissing "New SIM card detected" now really sticks — on the modem, not just in your browser.** The dismissal used to be remembered only by the browser you clicked it in, so the banner came back on your phone, on another laptop, or after clearing site data. It's now stored on the device per SIM and survives reboots and updates (the old dismiss request quietly failed on the device and reported success anyway; it's been replaced with a proper privileged write).
- **The wrong phone number no longer sticks around after a SIM swap.** If you switched to a SIM with no number provisioned — common on prepaid and data-only cards — QManager kept displaying the *previous* SIM's number. It now correctly shows "No number provisioned" instead (also made the number parsing robust against carriers that put a comma in the SIM's name field).
- **Tightened permissions on the folder QManager's system helpers load code from.** This folder was world-writable, which could have let a local attacker on the device replace one of those files and have it run with full privileges. It's now locked down, and the fix re-applies itself on every update so existing devices are repaired automatically.
- **Tightened permissions on QManager's own program folder.** The folder holding the web console was world-writable, which could have let a local attacker swap in their own console program to be run with full privileges at the next start. Now locked down, and repaired automatically on update.
- **Tightened permissions on the HTTPS certificate folder.** The certificate folder and the public certificate were world-writable, which could have let a local attacker replace them and intercept your connection to the QManager web interface. Both are now locked down, with the private key restricted on every update.
- **Tightened permissions on the web root.** The folder QManager serves the web interface from was world-writable, which could have let a local attacker on the device tamper with the pages and scripts it serves. Now locked down, and repaired automatically on update.
- **Tightened permissions on QManager's settings folder.** The folder was world-writable, which could have let a local attacker swap out the settings file QManager's background services read at startup and have their own commands run with full privileges. Now locked down, and repaired automatically on update (the file itself was already protected — but on Linux, replacing a file only needs permission on the folder around it).
- **An update can no longer stop partway through.** On a small number of devices, a config check during the update could fail and take the whole update down with it — after QManager's services had already been shut down for the upgrade. That check now logs a warning and carries on.
- **Tightened permissions on the scheduling folder and the background-service settings file.** Both were writable by the web interface, which could have let a local attacker influence code that runs with full privileges. The settings file is repaired the moment you update; the folder is repaired on your **next reboot after** updating, since that's when QManager's boot setup runs.
- **All your known SIM cards now show up under "Tracked SIMs".** Updating could print `WARNING: failed to seed sim_registry.json` and leave every SIM except the one currently inserted missing from the list — and a missing SIM couldn't be dismissed. This update fills in the gaps automatically, and won't touch SIMs you've already dismissed or renamed (the setup step used a text-matching feature the device's JSON tool ships without; the real error was also being hidden behind that generic warning).
- **Connection Quality settings can save again if the file behind them is ever damaged.** If the probe-target file on the device was left in a bad state, every save under **System Settings → Connection Quality** failed with a write error while the page kept showing defaults as though nothing were wrong — with no way to recover from the web console. QManager now rebuilds that file instead of refusing to write, from both the Connection Quality page and the Watchdog that shares it (the save merges into a JSON object, so anything else aborted it; the merge is now shape-checked and never promotes an empty temp file over a live config — re-save your probe interval on **Monitoring → Watchdog** if it was lost).
- **A successful speed test can no longer report itself as failed.** With the dashboard tile and the Speed Test dialog both watching a run, whichever one collected the result first could delete the log the other was still reading, turning a finished test into an error card. Only one of them harvests the result now, and the other simply waits (a file lock around the result collection; the loser gets a "still running" answer and retries).
- **Devices whose speed-test tool never downloaded now repair themselves.** If the Ookla download failed when QManager was first installed — offline, or a flaky cellular link — Speed Test stayed permanently dead with no way back. Updates now retry the download every time, so an affected device fixes itself on this update (the download used to sit inside a step that in-app updates skip entirely).

- **Your inbox no longer vanishes when the modem is busy.** A failed refresh in **Cellular → SMS Center** used to blank the message list, even though the usual cause is another part of QManager holding the modem for a second. Your messages now stay put, with a notice and a "Stale, 09:14:02" tag saying how old they are.
- **Reply is no longer offered on messages you can't reply to.** Texts from senders like GLOBE, SMART or NDRRMC aren't phone numbers, so replying to them always failed at the modem. The button is now hidden for those, with a short line explaining why.
- **Selected messages could be deleted after you filtered them out of sight.** Ticking a message under **All**, then switching to **Unread** or typing in the search box, left it selected and invisible — and "Delete N selected" still deleted it. Changing the tab or the search now clears your selection, and deletes only ever touch messages you can currently see.
- **Refresh no longer loses your place in the inbox.** Pressing Refresh in **Cellular → SMS Center** rebuilt the whole card, wiping your tab, your search, your sort order, your ticked messages and the page you were on. It now refreshes the messages underneath you and leaves everything else alone.
- **The Refresh and Retry buttons now show they're doing something.** Both used to sit completely still while the modem was being read, so the only way to tell was to wait; they now spin, and a second press can't stack another read on top of a modem that's already busy.
- **SMS Center and SMS Forwarding now really do speak all five languages.** Both pages had shipped with around a hundred labels stuck in English for Simplified Chinese (简体中文), Traditional Chinese (繁體中文), Italian (Italiano) and Indonesian (Bahasa Indonesia); every one of them is now translated.
- **Save buttons no longer switch back to English mid-save.** Most Save and Apply buttons showed a translated "Saving…" and then snapped back to an English label when they finished — every one of them is now translated in all five languages.
- **The confirmation tick at the end of a save was never actually appearing on five pages.** TTL & Hop Limit, MTU, System Settings, Alerts and Watchdog all jumped straight from "Saving…" back to the plain button, so you had only the toast to go on. All five now show the tick properly (each page rebuilt its form the moment the save's refresh landed, discarding the button's state in the same frame it was set).
- **Saving on Alerts or Watchdog no longer throws you back to the first tab.** Editing your email settings under **Monitoring → Alerts** and pressing Save used to dump you back on **Routing**; **Monitoring → Watchdog** bounced you to **Detection**. You now stay where you were, and your show-password toggle and place in the recovery list survive too.
- **Saving no longer makes the activity lists blink and reload.** Every save under **Monitoring → Alerts** and **Monitoring → Watchdog** quietly re-requested the activity log from the modem and flashed it back to loading placeholders. Both now sit still, and each save costs one fewer round trip to the device.
- **Replacing an app password now clears the box.** Under **Monitoring → Alerts**, typing a new email app password or Discord bot token over an existing one left it sitting in the field with Save still lit, as though it hadn't gone through. Both are cleared on a successful save now.
- **System Settings no longer re-animates itself every time you save.** The whole card faded and slid back into place after each save; it now simply updates, which also means it stops moving for anyone who has reduced motion switched on.
- **SMS Forwarding no longer says "off" before it has read your settings.** If the first read came back empty, **Cellular → SMS Forwarding** drew the form with the switch off and the number blank, which looked like a definite answer. It now says it couldn't load your settings and offers a retry.
- **The destination field on SMS Forwarding is the right shade in dark mode.** It had been drawing a slightly wrong fill on every dark-mode visit — close enough to look deliberate — and now matches every other field in QManager.
- **SMS Forwarding highlights what you've changed.** Switch forwarding on or edit the destination number and that row lights up until you save, so an unsaved change is visible rather than being implied by the Save button waking up.
- **A long forwarding error no longer runs past the edge of its card.** Delivery failures under **Cellular → SMS Forwarding** show the modem's own error text, which can be one unbroken string; it now wraps inside the notice instead of overflowing it.
- **Screen readers no longer read the whole SMS page aloud on every change.** Both **SMS Center** and **SMS Forwarding** announced their entire contents on every filter change and every background refresh; only the things that actually change — the relay's state, a delete in progress, a read failure — are announced now.

- **The Back button no longer traps you after being sent to the login page.** Opening a page while signed out sends you to **Login**, but the page you came from stayed in your history — so pressing Back bounced you straight to Login again. That redirect no longer leaves a history entry.

- **Features you switch on no longer quietly switch themselves off at the next reboot.** Turning on **Monitoring → Watchdog**, **Cellular → SMS Forwarding**, tower failover or Discord alerts could report success, visibly start working, and then be gone after a restart — most often on a modem where Tailscale had been installed or updated at some point (that left the system partition read-only for the rest of the session, so the boot entry silently failed to write). If it genuinely can't be saved for boot now, the page tells you instead of pretending.
- **Devices that already lost a setting this way repair themselves on this update.** If your Watchdog, SMS Forwarding or tower failover stopped surviving reboots, installing this release turns them back on from your saved settings — previously the loss was carried forward through every future update.

- **A band-lock error no longer appears under all three radio types.** If an LTE lock failed on **Cellular → Band Locking**, the same red message was also printed under 5G NSA and 5G SA, leaving you to guess which one actually failed. The error now shows only on the card you pressed.
- **Applying one band lock now pauses the other two.** All three cards go quiet while any lock is being applied, instead of letting you fire a second one seconds later — which used to cancel the safety net watching the first, leaving that change unmonitored for the rest of its 30-second window.
- **Tower Locking's switches work again when signal failover is on.** With failover enabled, both the 4G and 5G lock switches on **Cellular → Cell Locking → Tower Locking** simply refused to do anything, claiming "Signal quality check is running, please wait" — a check that never finished, because the tower failover watcher runs for as long as a lock is in force. The page now only blocks a second lock while one is genuinely being sent.

- **A second LTE channel could linger after you narrowed your lock.** If you locked two channels on **Cellular → Cell Locking → Frequency Locking** and later dropped to one, the page kept showing the old second channel — and quietly sent it again on your next apply. The list is rebuilt from what the modem reports now, so a channel it no longer holds disappears.
- **Refreshing your lock state no longer blanks the page.** Pressing **Refresh** used to tear the whole page back to grey placeholders; it now keeps your numbers on screen and just spins the button, with an **as of 14:32** stamp saying how old the reading is (the lock state isn't polled, because reading it costs the modem three commands on the same channel everything else uses).
- **A control that can't do anything now says so before you press it.** **Use current** stayed lit with no carrier to copy and then fired a toast *after* the click to tell you nothing had happened; it's now disabled and labelled **No carrier on air**.
- **A half-finished 5G entry can no longer be silently thrown away.** An ARFCN typed without a subcarrier spacing used to be dropped on apply with nothing said about it — entries are now added as a complete pair or not at all.

- **A running cell scan no longer looks like a broken modem.** A full sweep holds the radio for up to three minutes, and during that time the dashboard's background reader kept firing commands that all timed out — while the connection watchdog, which reboots the modem when it thinks the link is dead, never knew a scan was happening at all. The scanner now raises a maintenance flag for the length of the sweep, so both stand down until it finishes (the flag was described in QManager's own docs but had never actually been created; it also expires by itself after five minutes if a scan is killed).
- **An antenna with no reading no longer gets graded "Bad".** Cells the modem reported without a signal measurement were shown with the red **Bad** chip — a verdict where there was no measurement at all. They now read **No data** in grey, on both scanner pages.
- **A scan that dies mid-run now says so.** If the modem stopped answering partway through, the page sat on "scanning" indefinitely; it now reports the run as lost after about ten seconds and lets you start again.
- **The neighbour scanner picks up four fixes it had been missing.** It was a copy of the main scanner made before those fixes landed, so it alone lacked readable column names, column hiding on narrow screens, the filtered-result count, and a polling fix that could leave it reading stale results. Both pages now share the same code, so they can't drift apart again.
- **Frequency Calculator errors are no longer stuck in English.** Messages like "Please enter a valid number" were built as plain English text deep in the maths rather than as translatable labels, so they stayed English in every language.
- **Starting a neighbour read during a full sweep now tells you so, straight away.** Both pages under **Cellular → Cell Scanner** share the modem, so a read fired during a sweep used to sit there and fail confusingly about sixteen seconds later. You now get an immediate "a full band sweep is using the modem, it can take up to three minutes" — and the same in reverse if you try to sweep during a read (the two pages now share one lock, so only one scan can ever hold the radio).
- **A failed scan's error no longer disappears when you reload or open a second tab.** The message was deleted by whichever tab read it first, so everyone else saw a blank "nothing has run yet" page for a scan that had definitely failed. The error now stays on screen until your next scan clears it.
- **A half-blocked neighbour read no longer reports "complete — 0 cells".** If the modem was busy for one half of the read and the other half came back empty, the page announced a clean, confident empty result. It now says the read was incomplete and which half was blocked, so you know to run it again.
- **Switching language mid-scan no longer leaves the page stuck.** Changing your language while a sweep or a neighbour read was running silently stopped the page from watching it — the scan carried on for its full three minutes with nothing on screen ever updating. Both pages now keep tracking a run through a language change.

- **Retrying a failed read no longer throws away your unsaved changes.** On **Cellular → Cellular Basic Settings**, pressing **Try again** — or simply saving — used to silently revert every setting you'd staged but not yet applied. Your edits now survive both; a refresh can only update rows you haven't touched.
- **Your 5G rate limits can no longer break the page.** If your carrier reported an incomplete 5G rate line, **Cellular Basic Settings** failed to load at all rather than skipping the bad entry (a shell error was being printed into the page's own data).
- **Two open tabs no longer scramble your rate limits.** Loading Cellular Basic Settings in two places at once could mix both readings together and show rates that belonged to neither.
- **QManager no longer claims you're on LTE when it doesn't know.** If the modem's network reading couldn't be understood, the connection was reported as LTE regardless — a guess that looked exactly like a real 4G attach. It now says **Unknown** instead.

- **Changing two carrier-profile settings at once no longer loses one of them.** Switching automatic selection off *and* picking a bundle in the same go used to apply whichever change the page noticed first and silently drop the other. Both are now written in order, and the bundle only goes out once the switch has landed.
- **An IMEI that can't possibly be valid is now refused before it's written.** **Cellular → IMEI Settings** only checked that you'd typed 15 digits, so a number the network will always reject could be written to your modem — and you'd only find out after a reboot. Both the main and backup IMEI fields now run the proper checksum and say what's wrong under the field.
- **"No Blocked Networks" no longer means "we couldn't check".** If **Cellular → Blocked Networks** failed to read your SIM, it showed the reassuring all-clear anyway — a page whose only job is reporting a problem was reporting no problem when it had no data at all. It now says it couldn't determine the list, and offers a retry.
- **Clearing your blocked-network list now asks first.** The button wrote straight to your SIM with no confirmation and no undo; it now opens a short dialog that says exactly what it will do (nothing here reboots your modem).
- **Network Priority no longer hides a failed read.** If the modem didn't answer, the page showed an empty list with a live Save button beside it — indistinguishable from a modem that genuinely reported nothing. It now says the read failed and offers **Try again**, with a separate screen for the genuine empty case.
- **Network Priority now knows whether you've changed anything.** Save used to be permanently lit, and pressing it with nothing changed answered with "No changes to save" *after* a round trip to the modem. A pending-changes bar now appears only when your order actually differs, with a Discard beside it.
- **4G no longer renders green and 3G red on Network Priority.** The technology rows were coloured with the same green and red QManager uses everywhere for "healthy" and "broken", so a perfectly fine 4G row looked like good news and a working 3G fallback looked like a fault. Each row now wears its radio's own colour instead.
- **A failed AT command is no longer reported as a success.** QManager was checking for modem rejections in the one place they never appear, so a command the modem refused could still be shown as applied and saved as though it had worked. Every affected page now checks the real result, and writes confirm the modem's own reply before reporting success (swept across every affected script, most visibly Tower Locking's schedule, IP Passthrough, IMEI Settings and the MBN carrier bundles).
- **The watchdog no longer undoes a working SIM failover.** After switching to your backup SIM, **Monitoring → Connection Watchdog** could decide the slot was empty and switch straight back to the dead SIM, because it asked the modem whether the card was ready before the card had finished starting up. It now asks up to three times, a second apart, and tells you in the log if the SIM is present but locked by a PIN.
- **Frequency Locking now refuses to write when it can't confirm your tower lock.** If **Cellular → Cell Locking → Frequency Locking** can't read whether a tower lock is active, it now says so and blocks Apply instead of quietly going ahead, because the two locks must never be stacked (the page distinguishes "a tower lock is holding this" from "we couldn't read it", and the modem-side check refuses independently).
- **Band Locking no longer shows "Locked, 0 bands allowed" when it simply couldn't read your modem.** If the band read failed, **Cellular → Band Locking** drew a normal-looking page claiming every radio was restricted to nothing. It now says plainly that it couldn't read your bands, offers a retry, and freezes the band grids so you can't accidentally write a new lock over one it never showed you.
- **Setup no longer skips past a band lock that failed.** The band step in the first-run wizard could move on to the next screen even when the modem rejected the lock. It now applies each radio type in turn, lists which ones failed, and retries only those.
- **Tower Locking no longer tells you it's unlocked when it simply couldn't ask.** If the modem didn't answer, **Cellular → Cell Locking → Tower Locking** showed a confident green **Unlocked** chip, and reported "Keep lock after reboot" as switched off — a setting nobody had touched. Each card now says **Not reported** for the reads that failed, and keeps showing the ones that worked.
- **A tower lock you just applied is no longer undone half a minute later.** Locking bounces the modem for a few seconds, and the signal watchdog could read that brief drop as your signal collapsing and release the lock — after the page had already told you it worked. Writes now mark themselves for 30 seconds and the watcher sits that window out.
- **Losing signal readings no longer clears your tower locks.** If the modem stopped reporting a signal for about a minute, the watchdog treated "no reading" as a reading of 0% and released both locks, logging a percentage it had made up. No reading now means no decision.
- **The failover chip tells the truth in two more situations.** **Released — weak signal** could never appear at all, because the trace it reads was being deleted the instant it was written; and a watcher that had stopped after a failed release still showed a green **Watching**. You now see **Released — weak signal** when a failover has fired, and a red **Not guarding** when the safety net should be out and isn't.
- **Clearing a tower lock no longer reports a failure for something that worked.** If the boot-persistence switch couldn't be turned off afterwards, the whole unlock came back as an error even though the modem was already unlocked. It now confirms the unlock and warns separately that it may re-arm at the next reboot.
- **Six controls on Tower Locking were invisible.** The cell, channel and threshold fields were painted the exact colour of the panel behind them, so there was nothing on screen showing where the box was. Every field now sits one shade above whatever it's on, in both light and dark mode.
- **Rounded corners render at their intended size again.** Cards, fields and loading placeholders across the app were falling back to a smaller radius than the design calls for, so a placeholder didn't quite match the card that replaced it (about 174 places).
- **A reboot can no longer leave the web interface unreachable.** On some boots a second, empty web server bundled with Entware started first and took the port, so QManager wouldn't load on either http:// or https:// until you fixed it over SSH. The installer now disables that one for good (it decided whether to start by looking only for a process *named* `lighttpd` — a check QManager's own startup happened to satisfy on some boots and miss on others).

- **The Traffic Engine page no longer says "Stopped" while the engine is running.** The status card read the engine's state through a privilege ladder the web interface's user doesn't have on every modem model, so the card could report Stopped — with a live packet counter beneath it. The page now checks for the engine's actual process first, which works everywhere, and only asks systemd for finer detail (Restarting / Error) when that query is permitted.
- **"Packets processed" no longer resets to zero once a minute.** The Traffic Engine's housekeeping timer re-created its redirect rule once a minute even when nothing was wrong, wiping the counter each time; traffic totals would visibly collapse mid-stream. The rule is now left alone unless it's actually missing.
- **Traffic Masquerade lost a button it didn't need.** The tab kept an Apply button from the design this page was ported from, where a text field needed submitting; here the switch saves the moment you flip it, so the button is gone — matching the Video Optimizer tab.

## 📥 Installation

### Upgrading from v0.1.13

**System Settings → Software Update** → Download → Install. No SSH/ADB needed. All settings preserved.

### Fresh Install

> **No SimpleAdmin required.** QManager installs completely standalone — you do **not** need to install (or uninstall) SimpleAdmin or the RGMII toolkit first. The installer bootstraps everything itself (Entware, web server, users, services).

SSH or ADB into the modem and run:

```sh
curl -fsSL -o /tmp/qmanager-installer.sh \
  https://github.com/dr-dolomite/QManager-RM520N/raw/refs/heads/main/qmanager-installer.sh && \
  bash /tmp/qmanager-installer.sh
```

No `curl`? Use `wget` — the installer works either way:

```sh
wget -O /tmp/qmanager-installer.sh \
  https://github.com/dr-dolomite/QManager-RM520N/raw/refs/heads/main/qmanager-installer.sh && \
  bash /tmp/qmanager-installer.sh
```

## 💙 Thank You!

Bug reports and feature requests welcome on [GitHub Issues](https://github.com/dr-dolomite/QManager-RM520N/issues).

Like what's new? QManager is built and maintained for free — if these updates have made your setup a little better, you can show your support via [Wise](https://wise.com/pay/business/blackcatdev?currency=USD) or [PayPal](https://paypal.me/iamrusss). Every bit helps keep this project alive. [GitHub Sponsors](https://github.com/sponsors/dr-dolomite) works too.

**License:** MIT + Commons Clause — **Happy connecting!**
