# MEMORY.md index

- [URCs are uncapturable — nothing holds /dev/smd11](urc_capture_impossible_no_smd11_listener.md) — zero `/proc/*/fd` hits for smd11; atcli opens per-command, so `AT+QSIMSTAT=1` URCs are dropped AND risk interleaving into unrelated qcmd responses; always prefer the vendor's `?` read form + an existing poller tier

- [Rootfs ro-remount probe recipe + EROFS exit codes](rootfs_ro_remount_probe_recipe.md) — `mount -o remount,ro /` round-trips cleanly (no EBUSY, nothing notices); `ln -sf` AND `rm -f` both return **1** under EROFS (busybox -f masks only ENOENT); probe symlink must be named `qmanager*` or the sudoers grant fails first

- [/tmp ownership + protected_regular + PID churn](tmp_ownership_protected_regular_facts.md) — protected_regular=1 blocks ROOT from O_CREAT on www-data-owned /tmp files (no cap override; read-only opens exempt, which is why qcmd's `9<lock` works); protected_fifo does NOT exist on this kernel; PID churn measured 100/s → 32768 wraps in 325s; orphaned `*.tmp` siblings are the fingerprint of a failed cross-UID `mv`

- [APN COPS-bracket live experiment facts](apn_bracket_live_experiment_facts.md) — COPS=0 returns in 0.16s (NON-blocking, no poller starvation), reattach ~1.3-4s, no signal_lost event but a spurious band_change; the bracket hops the WAN across rmnet_dataN yet data usage reads rmnet_ipa0 so it's immune; clean-control payload `{"scenario":{"default":""}}`; busybox `date +%s.%N` prints literal %N

- [current_settings.sh is 0.22s, not 2-3s](current_settings_is_fast_not_3s.md) — the hook comment and sim-profiles.md are 10x stale; all 5 Custom-Profiles endpoints fired concurrently finish in 0.52s wall, so there is no AT cost worth combining; includes the /proc/uptime timing recipe and the `sudo -n` gotcha

- [Every boot starts in 1970; timers arm inside that window](boot_clock_1970_window.md) — no battery RTC, ql_time_daemon steps the clock ~56y forward at boot+~25s, but timers.target arms at boot+6.3s; includes the no-journald proof recipe (/var/log/messages.0 shows the jump in one file)

- [eUICC in slot 2: probe facts](euicc_slot2_probe_facts.md) — hot-swap detect is OFF (`QSIMDET 0,1`) so a mid-session insert is invisible until a power cycle; ISD-R AID opens via AT+CCHO; blank-profile card reports all-1s ICCID/IMSI + CEREG denied

- [ES10c APDUs over AT+CGLA WORK — no lpac needed to read](euicc_es10c_apdu_over_cgla_works.md) — GetEID/GetProfilesInfo/GetEUICCInfo1 all returned 9000 with working hex strings; `<len>` is HEX CHARS not bytes; card is a strict TLV parser (6A80 on malformed tag list); legacy 80CA GET DATA = 6D00
- [Device time & /etc volume facts](device_time_and_etc_volume.md) — glibc 2.31 (ignores /etc/TZ), empty /usr/share/zoneinfo vs Entware /opt/share/zoneinfo, /etc is rw persistent ubi2_0 but root-only for www-data, NITZ off by default
- [Public endpoints & status cache](public_endpoints_and_status_cache.md) — auth is script-level (`_SKIP_AUTH`, lighttpd does NOT gate cgi-bin); qmanager_status.json shape == RM551E so public/overview.sh jq ports verbatim, zero live AT; hostname=sdxlemur via /proc; docroot is **/usrdata/qmanager/www** (verified 2026-08-01 from the running lighttpd's own config; "/opt/share/www" here was wrong and has misled three separate runs)
- [Live observability & SIM hardware](live_observability_and_sim_hardware.md) — journald is empty device-wide (journalctl useless; use single /tmp/qmanager.log); dual-SIM AT matrix (QUIMSLOT (1,2) yes, QDSIM no, QSIMDET/QSIMSTAT active-slot-only, device on slot 2)
- [ICMP reachability is VARIABLE per-SIM](icmp_unreliable_on_cellular.md) — 2026-07-19 ICMP to 8.8.8.8=100% loss; 2026-07-20 SAME modem=0% loss + working IPv6. Not an invariant. HTTP/204 daemon's real value is tri-state (connected/limited/disconnected), not ICMP-avoidance
- [ICMP+IPv6 both work on test SIM 2026-07-20](icmp_now_works_on_test_sim.md) — contradicts the blanket "carrier drops ICMP on rmnet" claim; don't retire qmanager_ping on that basis — you'd lose captive-portal/billing-wall (limited) detection ICMP can't do
- [File transfer to modem](file_transfer_to_modem.md) — no SFTP/SCP on dropbear; exec cmd >~8KB aborts the connection (68536 packet ceiling); push files via chunked base64 (7KB chunks, one session) + sha256 verify; PS tool blocks local `rm /tmp/...` strings, use `unlink`
- [Service unit naming: dash vs underscore](service_unit_naming_dash_vs_underscore.md) — units are `qmanager-poller.service` (dash), binaries are `qmanager_poller` (underscore); `is-active` with underscore falsely reports inactive; watchcat unit exists but is NOT boot-enabled
- [systemctl is-enabled: /lib vs /etc](systemctl_is_enabled_lib_vs_etc.md) — is-enabled reports "disabled" for QManager's /lib manual boot symlinks (only /etc config-path symlinks count as enabled); migrating svc_is_enabled→systemctl is-enabled without moving symlinks to /etc = backward-compat landmine
- [/etc/profile.d absent; login PATH lacks /opt/bin](etc_profile_and_login_path.md) — stock RM520N-GL has NO /etc/profile.d dir (cp into it fails "No such file or directory"); /etc/profile sources it but `[ -d ]`-guarded; login-shell PATH has no /opt/bin; /etc is rw UBIFS
- [crond binary present but daemon DEAD](crond_binary_present_but_daemon_dead.md) — /usr/sbin/crond (busybox) + /usr/bin/crontab EXIST so `which crond` lies; no systemd unit/init.d/wants-symlink starts it, ps shows no daemon, crontab dir empty+world-writable → any crontab write silently no-ops; use systemd .timer + arm helper
- [Root-poller /tmp flags unwritable by CGI](root_poller_tmp_flags_unwritable_by_cgi.md) — poller-created /tmp/qmanager_* flags are 644 root:root; www-data CGI (lighttpd server.username) CANNOT overwrite them though it CAN create NEW /tmp files (1777); dismiss/ack `> "$FLAG"` writes silently no-op — verify write or use root helper. Confirmed for sim_swap banner 2026-07-22
- [/tmp cross-UID truth table (sticky + fs.protected_regular=1)](tmp_cross_uid_rules_protected_regular.md) — only root-owned 0666 is writable by both UIDs; a www-data-owned /tmp file blocks ROOT with no CAP override; chmod never grants unlink; and atomic tmp+rename silently destroys qmanager_setup's pre-seeded owner/mode (live proof: profile_state.json seeded www-data 0666, observed root:root 0644)
- [Stale .env SSH password](stale_env_ssh_password.md) — 2026-07-24 dropbear rejected .env MODEM_SSH_PASSWORD (clean parse, device reachable) → device pw likely rotated via the new password-reset feature; stop + ask user to refresh .env, don't retry
- [/etc/qmanager is 0777 www-data — CGI writes configs directly](etc_qmanager_is_0777_www_data_writable.md) — dir is drwxrwxrwx www-data:www-data and nearly all configs are www-data-owned; no root helper/sudoers needed for persistent JSON. The write blocker is /tmp root-owned flags ONLY. /etc/sudoers.d does not exist — use `sudo -l -U www-data`
- [Phone number already in poller](phone_number_already_in_poller.md) — AT+CNUM works on the test SIM and the poller publishes it at `.device.phone_number` (boot batch + every-cycle QTEMP/COPS/QUIMSLOT/CNUM/CPIN refresh); never add a new AT read for MSISDN
- [Live serving mode is VARIABLE, re-probe it](live_ca_state_globe_sim.md) — was LTE-only, but 2026-07-30 the SAME modem ran full EN-DC (LTE B3 PCC + LTE B1 SCC + NR5G B41 SCC); NR SCC rows carry 8 fields vs LTE SCC's 13 vs PCC's 10; dedup on earfcn, not PCI
- [Per-antenna signal has THREE inactive-port sentinels](per_antenna_signal_sentinels.md) — RSRP `-140` and NR SINR `-20` pass through the poller as real numbers (only `-32768`/empty become null, and -32768 is never actually seen); both land exactly on a threshold floor so the UI renders "poor + empty bar" instead of "no data"
- [Poller cycle is 3s; blank band reads break the event chain](poller_cycle_3s_and_blank_band_reads.md) — POLL_INTERVAL=2 is a post-cycle sleep, real Tier-1 cadence is 3s; out-of-service samples blank lte_band and snapshot the blank as prev, so band_change transitions look asymmetric (20x "B8→B41", zero "B41→B8"); read transitions as an in/out-degree graph, confirm blanks via `grep "no +QENG: lines" /tmp/qmanager.log`
- [Speedtest live-probe facts](speedtest_live_probe_facts.md) — a full run costs ~400MB cellular (truncated 11s run still 136MB); status.sh DELETES the NDJSON progress file on harvest so progress shapes are unrecoverable after completion; /root and /var/www both absent so Ookla config never persists and the license banner re-runs every call; restoring a /tmp/qmanager_speedtest_* file as root freezes the cache (sticky bit)
- [jq paths(scalars) drops false/null](jq_paths_scalars_drops_false.md) — the `select` tests truthiness, so `isVpn:false` vanishes from the key map while `persisted:true` survives; never use it to prove a field is absent — check the raw JSON
- [AT+QCAINFO is per-cycle, NOT Tier 2](qcainfo_is_per_cycle_not_tier2.md) — poller header comment + carrier-aggregation.md both say Tier 2 and are WRONG; carrier_components refreshes every ~3.7s; measured 0 empty arrays in 103 consecutive polls; includes the census recipe + the nested-`\"` jq trap
- [AT mutex duty cycle ~0.03% + safe contention recipe](at_mutex_duty_cycle_and_contention_recipe.md) — measured 1/3053 samples busy over 30s with the poller live, so "will one more AT read contend with the poller" is always NO; only QSCAN holds >5s. Force contention read-only with `( flock -x 9; sleep 16 ) 9</tmp/qmanager_at.lock &` — never issue a real QSCAN. Also: qcmd echoes the command on success, so empty stdout ⇔ failure ⇔ rc=1, never ambiguous
- [Posh-SSH connection recipe](posh_ssh_connection_recipe.md) — single-call probe pattern + 4 gotchas (env not persisted, don't name fn `R`, no -TimeoutSecond, busybox df quirk); running lighttpd = /opt/sbin/lighttpd -f /usrdata/qmanager/lighttpd.conf, docroot /usrdata/qmanager/www (NOTE: public_endpoints memory's "/opt/share/www" is WRONG)
- [APN testing on the Globe SIM](apn_testing_on_globe_sim.md) — `http.globe.com.ph` is granted verbatim so it's a safe non-default test APN; pre-flight with a manual bracket first; carrier default reads UPPERCASE only when configured APN is blank, so skip-checks must case-fold
- [Walled-garden triage recipe](walled_garden_triage_recipe.md) — 3-probe ICMP/DNS/TCP signature table separating carrier-filtered-ICMP vs walled garden (instant TCP RST + carrier private resolver) vs real outage; local iptables ruled out by construction; rmnet_data0 has NO IPv6 so the v6 fallback is dead weight
- [Boot chown defeats ownership pins in /etc/qmanager](boot_chown_defeats_ownership_pins_in_etc_qmanager.md) — qmanager_setup's per-boot `chown -R www-data:www-data /etc/qmanager` has no carve-out, so install-time root:root pins last only until reboot; prove it via ctime(boot) vs mtime(old); root:root entries there are root-daemon recreations, not pins
- [qcmd arm 3 exits 0 on non-empty output with no OK/ERROR](qcmd_passthrough_branch_exits_zero.md) — the ONE false-success path; `rc`/`-z` guards are enough for a QUERY but never for a WRITE, which needs a positive `*OK*` assertion or a read-back
- [Band locking reads locked and supported from two independent sources](band_locking_two_source_split.md) — locked = live `ue_capability_band` via current.sh; supported = boot-only `policy_band` cached in /tmp/qmanager_supported_bands.env; the two are byte-identical when nothing is locked
- [Authenticated CGI probing + the qcmd stub trick](authenticated_cgi_probe_and_qcmd_stub.md) — port 80 301-redirects to https so plain http probes look broken; reuse a live token from /tmp/qmanager_sessions/ (validation is side-effect-free) instead of _SKIP_AUTH; and shadow `qcmd` with a shell function to exercise AT-failure branches with zero device footprint

- [Quectel version-file labels are column-aligned, not single-space](quectel_project_version_double_space_labels.md) — BOTH devices write `Branch  Name:` (two spaces) and `Project Rev :`; the poller's `grep "^Branch Name"` matches on neither, so `detect_orientation_from_soc` always returns "normal" while its unit-test fixture (one space) passes
