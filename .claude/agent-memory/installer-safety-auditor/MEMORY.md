- [/etc/data/ layout and QCMAP behavior](project_etc_data_layout.md) — persistence facts, QCMAP non-wiping, www-data staging subdir pattern
- [Phase 5 validation must use real CGI path](feedback_validation_must_use_cgi_path.md) — root-via-SSH masks permission bugs; always test through lighttpd for /etc/data/ writes
- [Live mount topology: rootfs vs ubi2_0](project_mount_topology.md) — /etc, /opt, /usrdata, /data, /cache, /systemrw, /persist are ALL separate always-rw ubi2_0 mounts, NOT rootfs; only /usr, /bin, /lib are rootfs (ubi0, ro-on-stock-boot)
- [OTA always skips package installs](project_ota_skips_packages.md) — qmanager_update passes --skip-packages, so install_dependencies()/opkg calls never run on OTA; new package deps need an unconditional step like remove_conflicts()
- [Sudoers pattern: bare path, validation lives in the helper](feedback_sudoers_arg_validation_pattern.md) — every qmanager_* helper is granted with no arg restriction in sudoers by design; audit the helper's own input validation, not the sudoers line
- [$WWW_ROOT is wiped every install/OTA, root-owned](project_www_root_wipe_and_ownership.md) — install_frontend() rm -rf's everything under www root except cgi-bin on every run; any externally-written content needs to live outside www root and be re-linked in
- [lighttpd.conf is a wholesale template, mod_alias not loaded](project_lighttpd_conf_is_a_template.md) — runtime edits to lighttpd.conf don't survive OTA; new lighttpd modules need an unconditional opkg install step, same trap as project_ota_skips_packages
- [qmanager_watchcat daemon architecture + live-confirmed tmpfs bug](project_watchcat_daemon.md) — SIM-failover state file lives on tmpfs and is lost on Tier-4 reboot; must relocate to /etc/qmanager before Tier-4 SIM-failover work ships
- [Pre-existing installer gaps found incidentally](project_preexisting_installer_gaps.md) — qmanager_console_mgr remounts rootfs ro without sync; sudoers systemctl wildcard is broader than needed — both out of scope unless those files are touched
- [Config-file pruning asymmetry](project_config_pruning_asymmetry.md) — /usr/bin, systemd units, /usr/lib/qmanager/*.sh, and CGI tree are all self-cleaning (filesystem-diff or wipe-resync); /etc/qmanager (CONF_DIR) is additive-only and NEVER auto-pruned — orphaned config needs an explicit prune step
- [install_file() ELF-sniffs to skip CRLF strip; qm_config_get() has safe default fallback](project_install_file_elf_and_config_defaults.md) — binary->script swaps at a stable filename need zero installer changes; new config keys are safe on upgraded devices only if every read call site supplies its own default
- [Ping-daemon retirement audit (2026-07-19)](project_ping_daemon_retirement.md) — Rust ELF -> POSIX shell qmanager_ping swap PROCEED verdict; live-confirmed ping_profile.json schema, no sudoers entry, migration precedent (migrate_ping_environment/prune_stale_ping_environment) to reuse
- [systemctl enable/is-enabled split-brain vs manual /lib symlinks (live-confirmed 2026-07-20)](project_systemctl_enable_split_brain.md) — enable DOES work but is-enabled doesn't see the existing manual /lib wants-symlinks; a platform.sh-only systemctl migration would misreport enabled state and orphan legacy symlinks on disable
- [Auto-update timer arming spec](project_auto_update_timer_arming.md) — install_rm520n.sh already config-gates qmanager-auto-update.timer via manual /lib symlinks; a live-arm root helper must reuse that exact mechanism, not systemctl enable, or it splits from installer/OTA state
- [CONF_DIR recursive chown at install:1124](project_conf_dir_recursive_chown.md) — chown -R www-data:www-data /etc/qmanager runs every install/OTA, reverting any per-file root:www-data plan and making every file in the dir symlink-swappable by www-data since it owns the directory too
- [email_alerts.sh/sms_alerts.sh basename collision](project_alert_lib_cgi_basename_collision.md) — same-named lib (poller-sourced, load-bearing) vs CGI endpoint (safe to retire) in different dirs; "delete legacy alert scripts" briefs must use full paths or a builder will kill live alert delivery
- [crash.log dual-writer audit (2026-07-20)](project_crash_log_dual_writer_audit.md) — BLOCKED verdict on chmod 664 group-write for crash.log; recommended a qmanager_crash_log_append root helper with symlink guard + enum-validated reason instead
- [rename() vs open(O_TRUNC) for two-writer files (2026-07-27)](project_two_writer_rename_vs_truncate_semantics.md) — mktemp+mv is symlink/ownership-safe in world-writable /etc/qmanager even across root/www-data writers; raw >/>> is not; use this to judge whether a root helper is actually required
- [sim_registry.json two-writer audit (2026-07-27)](project_sim_registry_two_writer_audit.md) — BLOCKED as specified: no root helper + PII (phone_number) in a world-writable dir; required root-owns-writes model with www-data mutating only `.dismissed` via helper or scoped key-merge
- [Shared /tmp flag + sticky-bit cross-UID unlink hazard (2026-08-03)](project_shared_tmp_flag_sticky_bit_hazard.md) — chmod 0666 does NOT grant delete rights under /tmp's 1777 sticky bit; a root-created flag can't be rm'd by www-data (or vice versa) — BLOCKED apn-attach-cycle's reuse of qmanager_recovery_active, which alert_engine.sh/events.sh key off to silence all alerts
- [Lib dir is a glob-install + full-reinstall OTA, not an enumerated list (2026-08-03)](project_lib_dir_glob_install_and_ota_wiring.md) — install_dir_flat() globs usr/lib/qmanager/*, OTA re-runs the whole installer (only --skip-packages is gated) — a new lib file needs ZERO installer/OTA edits
- [/usrdata/qmanager root dir is 0755 root:root, blocks www-data writes (live-confirmed 2026-08-03)](project_usrdata_qmanager_root_dir_blocks_www_data_writes.md) — apn.sh's WS6 sidecar mktemp+mv silently no-ops (apn_setting.json doesn't exist on device); a root writer landing there first is not a new clobber risk, just the first writer that can actually succeed
- [stop_services stops qmanager-setup; /tmp seed rules; qlog rotation breaks the log seed (2026-08-03)](project_qmanager_setup_restart_and_tmp_seed.md) — the RemainAfterExit latch is cleared every install/OTA so `start` was never a no-op; root:root 0666 is the only shared-/tmp ownership; qlog.sh:103-106 `mv` destroys it for qmanager.log
- [stop_services is unconditional; nothing reaches start_services without it (2026-08-03)](project_installer_stop_start_ordering.md) — main()'s unconditional block, OTA never passes --no-start, and `restart` == `start` on an inactive/nonexistent unit with `|| true`
- [Root cannot reclaim a www-data-owned /tmp flag (2026-08-03)](project_root_cannot_reclaim_www_data_tmp_flag.md) — protected_regular has no root override, so any verified read-back claim needs `rm -f` FIRST or a stale flag strands alert suppression until reboot
- [apn-attach-cycle Phase 5 re-audit: PASS (2026-08-03)](project_apn_attach_cycle_phase5_reaudit.md) — flag became root-worker-only per the Phase 1 blocker resolution, fail-closed sourcing confirmed in all 3 consumers, installer/OTA/sudoers/systemd/qcmd/watchcat all genuinely untouched
