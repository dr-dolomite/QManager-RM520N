- [SSH upload + tooling quirks for on-device testing](reference_ssh_upload_and_tooling.md) — SFTP unavailable, base64-chunk uploads, PowerShell sandbox blocks literal "rm" even in remote SSH command strings, jq/sudo confirmed available
- [/tmp sticky bit breaks cross-user mv in test seeds](feedback_tmp_sticky_bit_seed_as_owner.md) — seed throwaway config files as the same user (www-data) that will mv over them, or get a false-positive permission failure
- [/tmp cross-UID rules + stat/arithmetic facts](reference_tmp_crossuid_and_stat_facts.md) — /bin/stat is BusyBox and works under the stripped CGI PATH; /bin/sh arithmetic is 32-bit signed; root CANNOT write a www-data-owned /tmp file; mv fails, `cat >` preserves the inode
- [Verified BusyBox applet + sudo/visudo + volume facts](reference_rm520n_applet_and_privilege_facts.md) — install -d -m self-heals modes and accepts -o/-g; mktemp full-path→0600; [[:cntrl:]] works in ash case; printf is a builtin (no argv leak); visudo ONLY at /opt/sbin; st_dev map for atomic-rename claims
- [ping_profile.sh has a pre-existing malformed-config merge bug](project_ping_profile_malformed_config_merge_bug.md) — latent, not introduced by the profile-optional fix, confirmed present at HEAD too; worth a follow-up ticket
- [BusyBox mv onto a directory returns 0, nests file inside](reference_busybox_mv_onto_directory.md) — confirmed live; `test -f $dst` (not `-e`, not mv's exit code) is the only reliable "did this actually migrate to a regular file" check
- [BusyBox flock FD-form confirmed live (v1.31.1)](reference_busybox_flock_fd_form_confirmed.md) — `flock -x -n <fd>` on an already-open numeric fd works; a read-only `9<` fd is sufficient for `-x`; fd 9 survives `( cmd & )` backgrounding and outlives the parent's exit; sleep 0.2 measured real
- [`set -e` + command substitution: `x=$(f)` on nonzero DOES abort](reference_set_e_command_substitution_semantics.md) — only `cmd && ...`/`cmd || ...` position is exempt from -e, not a bare assignment; the safe library idiom is `val=$(fn || printf fallback)`; no `busybox` binary in this Windows sandbox, use `dash` as the local ash proxy
- [hw_profile.sh symlink-attack defenses confirmed live, zero divergence](reference_hw_profile_symlink_defenses_confirmed_live.md) — all 7 write_profile/self_heal hardening behaviors (symlink refusal, dir refusal, crash recovery, umask-000 chmod, no-op convergence) identical on BusyBox 1.31.1 and 1.29.3; `set -C` noclobber confirmed closing the TOCTOU race on both
- [GUI installer's wrap_command brace-group confirmed on both devices](reference_wrap_command_brace_group_confirmed_both_devices.md) — trailing-&, double-wrap-for-2>&1, and all 5 static preflight probes behave identically on ash 1.31.1 and 1.29.3; zero divergence found
- [RG501Q timeout IS a native busybox applet; only wget is Entware-only](reference_rg501q_wget_timeout_applet_provenance.md) — corrects a grouped "wget+timeout are Entware-only on RG501Q" assumption; timeout symlink mtime matches firmware build date, wget symlink mtime is post-provisioning; BusyBox wget rejects `--version`/`-V` on both devices, never parse that
- [BusyBox ash supports `${var:offset:length}` under actual /bin/sh on both devices](reference_ash_bash_substring_expansion_works.md) — CONFIG_ASH_BASH_COMPAT is on for both builds; a `#!/bin/sh` script using this bashism is NOT automatically broken, verify before flagging
- [BusyBox awk comma-split + alternation confirmed live (+CGCONTRDP fix)](reference_awk_comma_split_and_alternation_confirmed.md) — split()/gsub-on-array/tolower/next-in-pattern/`|` alternation all work on both 1.31.1 and 1.29.3; RG501Q's unquoted +CGCONTRDP wire format is a REAL live behavior, not just a fixture
- [F15: 3 bare mkdir -p sites are ALREADY 777 on both fielded devices](project_f15_bare_mkdir_p_confirmed_live_777.md) — BACKUP_DIR, /etc/profile.d, locales-packs; install shell's umask is 0000 (not the 022 a live SSH shell shows); mkdir -p no-op and install -d -m 0755 confirmed identical on both BusyBox builds
- [RG501Q-EU /etc/passwd is 0600 root:root, NOT world-readable (RM520N-GL is 0644)](reference_rg501q_etc_passwd_not_world_readable.md) — any by-NAME id/getent/awk-on-passwd lookup fails for a non-root caller there; broke qmanager_health_check's www-data self-detection branch (63bc6a9) — fix is a hardcoded uid-33 literal, never a dynamic name lookup
- [parse_serving_cell's manual hexval()/36-bit NCI awk rewrite confirmed on both BusyBox awk builds](reference_awk_manual_hexval_and_36bit_nci_confirmed.md) — 14/14 golden fixtures byte-identical; real 20-field LTE-only capture shows the CR-asymmetry note is EN-DC-branch-specific, not general
- [PowerShell `[A-Z_]+` env-key regex silently drops digit-bearing keys](feedback_powershell_regex_az_excludes_digits.md) — RM520N_*/RG501Q_* vars need `[A-Z0-9_]+`; symptom is a confusing downstream "null argument" error, not a regex complaint
- [RG501Q-EU `/opt/bin/timeout` is now GNU coreutils 9.9, not BusyBox](reference_rg501q_opt_bin_timeout_is_gnu_coreutils.md) — installed 2026-08-25 via Entware; qm_timeout's absolute-path `/opt/bin` probe finds and prefers it even under a restricted daemon PATH, so RG501Q-EU resolves `positional`, not the BusyBox-1.29.3-implied `legacy` form — don't predict `_QM_TIMEOUT_BIN`/`_QM_TIMEOUT_FORM` from BusyBox version alone
- [qm_timeout's pure-shell fallback branch preserves stdout through command substitution](reference_qm_timeout_fallback_stdout_capture_confirmed.md) — confirmed under dash: it always `wait`s the backgrounded child before returning, so `out=$(qm_timeout ...)` captures full output even on the fail-open path; not yet proven live on-device (neither target device exercises this branch today)
