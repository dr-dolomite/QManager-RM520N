- [A brief's "file.md:NNN" may live only in the shared checkout](reference_worktree_missing_uncommitted_docs.md) — worktrees carry committed state only, so uncommitted/untracked doc work is invisible there; diff against the shared copy before calling the brief wrong
- [SMS visual-verification status](project_sms_tonal_rebuild_visual_check_pending.md) — SMS Center cleared 2026-08-20 on a fixture route; Forwarding still unverified (needs a live backend), and the sibling warnings elsewhere stay open
- [1970 boot window / timer fire guard](reference_1970_boot_window_timer_guard.md) — any new systemd .timer must be checked against this; full mechanism + non-fixes list in scheduled-timers.md
- [Replace, don't append, on UI rewrites](feedback_replace_dont_append_on_ui_rewrites.md) — rewrite the anatomy in place; keep "do not restore" notes only for shapes someone would helpfully reinvent
- [AT+QNWLOCK "common/5g" answers from stored config, not live NR](reference_qnwlock_5g_answers_from_stored_config.md) — a full +QNWLOCK line comes back even on an LTE-only device, so a missing line is a FAILED read; settles the NR/LTE reader asymmetry someone will re-litigate
- [A documented guard with no writer](reference_documented_guard_had_no_writer.md) — `/tmp/qmanager_long_running` had three readers and zero writers for years; classify every hit as write/read/delete, and remember a writer in the tree ≠ a working guard on the device
- [i18n:check is a hard gate again (2026-08-12)](reference_i18n_check_now_hard_gate.md) — missing key/empty value exit 1; `--warn-only` carries tracked debt; the holes that remain are hardcoded literals, `defaultValue`, and interpolated keys
- [Release-notes bullets from earlier passes go stale](reference_release_notes_earlier_bullets_go_stale.md) — one active entry spans many passes, so a later redesign can falsify a bullet already in the file; grep and correct in place at every close
- [Locale packs are CRLF, and so are docs/*.md](reference_locale_packs_are_crlf.md) — the JSON round-trip guard aborts on all five packs, and an edit pass can flip a whole doc to LF where git diff hides it
- [A retired export stays greppable because a sibling family exports the same name](reference_same_export_name_in_two_shape_modules.md) — each `/cellular/` family owns its own `shapes.ts`; pin the module path or a doc's "it's gone" claim reads as wrong
- [Commit body is the archive; the reference doc is forward-looking only](reference_commit_is_archive_doc_is_forward_looking.md) — mine `git show` for invariants and "do not restore this" warnings, not the defect narrative
- [A class inside a `components/ui/**` primitive is invisible to family-scoped reasoning](reference_shared_primitive_classes_are_invisible.md) — it still renders on the page, so "not on this surface" needs the primitive opened or the page loaded
- [A jq `//` default masks an ABSENT key as a truthy sentinel, never null](reference_jq_default_masks_absent_key.md) — so "null when missing" in a doc can be structurally impossible; document a producer's COMPLETE key list as a contract
- [Connectivity probe shipped as Change A only — 3 approved follow-ups NOT landed](project_connectivity_probe_followups.md) — `fail_elapsed_sec` is emitted-and-unread by design, the inert `limited` branch is still present, and the Connection Quality route has zero locale keys
- [A `systemctl show` readback on a MOUNT unit cannot fail](reference_mount_unit_property_readback_cannot_fail.md) — systemd derives the ordering properties from mountinfo, so it passes on an untouched device; the inverse holds for hand-written .service units
