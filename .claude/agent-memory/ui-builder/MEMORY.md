# ui-builder memory index

- [Signal chip = identity, not quality](feedback-signal-chip-identity-not-quality.md) — NR blue / LTE violet fill, quality moved to the Material glyph's bar count; metric value tints stay green/amber/red for both radios
- [Approved off-ramp font sizes on the dashboard](feedback-signal-card-13px-off-ramp.md) — 13px signal rows, 44/52/26px speedtest numerals, 11/17px latency tile; the impeccable hook flags them every edit, keep them, don't add a suppression
- [Icon-Boundary Rule now covers `/` and `/login/`](project-icon-boundary-extended-preauth.md) — pre-auth routes are Material Symbols; `material-symbol-names.ts` is a multi-agent contention point, re-read before editing
- [Icon-Boundary Rule now covers `/cellular/` too, and the subset is short](project-icon-boundary-cellular-subset-gap.md) — SUPERSEDED on the glyph list; the boundary claim still holds
- [Material subset: grep the array, then REGENERATE](project-material-subset-gaps.md) — 109 glyphs; `icons:subset` reaches Google Fonts from here, so adding one is completable in-session (binary font diff), not a blocked two-part change
- [`resp.ok` cannot detect a CGI failure](reference-resp-ok-cannot-detect-cgi-failure.md) — `cgi_error` returns HTTP 200 with no `Status:` line, so only `data.success !== true` catches a rejected write; message is `detail || error`, and never advance from a `finally`
- [No prettier in this repo](reference-no-prettier-in-repo.md) — `bunx prettier --check` downloads a stock-default prettier that fails on untouched files too; gate is `eslint` + `tsc --noEmit`
- [Agent definition names the WRONG fonts](feedback-fonts-rethink-sans-jetbrains.md) — it's Rethink Sans + JetBrains Mono, not Euclid/Geist; sans+`tabular-nums` for values that change unprompted, mono only for identifiers
- [`t` in a polling hook's deps kills the interval](reference-t-identity-churn-kills-intervals.md) — no `bindI18n` override, so a language switch churns `t`, rebuilds the callback, re-runs the effect and stops polling forever; use a `tRef`, never an eslint-disable
- [Locale parity baseline is 100%](reference-locale-parity-baseline-is-100.md) — any en-only key addition = N×4 strict errors; delete retired keys from all five packs; locale JSON is CRLF so the round-trip guard must normalise or it always aborts
- [Throwaway `app/qm-preview/` fixture route](reference-visual-verification-fixture-route.md) — hash-driven state matrix + `@width` maxWidth for container queries; the 3 traps: `&`-backgrounded dev server looks dead but isn't, a wedged Chrome tab mimics an infinite render loop, and deleting the fixture breaks `tsc` until `.next` is cleared
- [`Input`'s `dark:`/`md:` classes survive `cn()`](reference-input-primitive-modifier-classes-survive-cn.md) — `dark:bg-input/30` + `md:text-sm` are not displaced by unprefixed overrides, so a SELECT_TRIGGER-shaped field reverts in dark mode and ≥768px; use a raw `<input>` + the shapes focus idiom
- [Standing explanatory copy gets cut](feedback-explanatory-copy-gets-cut.md) — cost paragraphs, pre-action hints and duplicated counts were all removed 2026-08-14 despite documented rationales; prefer conditional, state-scoped copy over permanent prose
- [Middot survives only in machine-voice runs](feedback-middot-only-in-machine-voice-runs.md) — `·` as generic glue was called out; label the value and let layout separate. Watch for shared keys whose call sites pre-prefix a param
- [Never `git stash` in a shared worktree](reference-never-stash-in-a-shared-worktree.md) — two agents hold uncommitted work in one tree; scope `bunx eslint <your paths>` instead of stashing to baseline a red repo-wide lint
- [Tag/Badge-shaped skeletons need `bg-accent` put back](reference-tag-skeleton-bg-transparent.md) — `tagVariants()` carries `bg-transparent` so the placeholder renders invisible; `badgeVariants()` carries the role fill so it pulses as a finished chip. Nothing lints it
- [A `dark:` fill override TIES — the trailing `!` is what wins it](reference-dark-fill-override-wins-by-source-order.md) — both sides compile to (0,2,0), so the win was Tailwind's alphabetical name sort until the important modifier made it constructed; `select.tsx` also needs the `dark:hover:` override
- [An honesty field must ship with its consumer](feedback-honest-backend-field-needs-its-consumer.md) — a declared-but-unread `service_disable_failed` turned a misleading error into total silence; and a `read_ok` guard on a boolean sink is a no-op, because a failed read and "not locked" are already the same value
- [Verify a brief's "already done at N sites" against `git show HEAD`](feedback-verify-brief-line-numbers-against-head.md) — a 2026-08-23 mid-task correction was counting my own uncommitted edits and would have reverted a correct fix
- [Custom `rounded-*` overrides win or lose ALPHABETICALLY](reference-custom-radius-overrides-lose-alphabetically.md) — VERIFIED in built CSS: `.rounded-field` is emitted before `.rounded-md`, so a Skeleton renders 6px not 20px; the fix is a trailing `!`
- [`aria-description` lints on a raw `<button>`, not on `<Button>`](reference-aria-description-lints-on-raw-button.md) — jsx-a11y cannot see a component's implicit role, so the existing Button precedent misleads; use `aria-describedby` + an sr-only span on hand-rolled controls
- [A `*_read_ok` sidecar flag fails OPEN; a tri-state `boolean | null` fails safe](reference-readok-sidecar-fails-open.md) — the sidecar leaves the primary `?? false` default reading "not active" at every consumer that does not thread the flag; the tri-state puts the fail-safe in the one gate everybody already reads
- [An `&`-backgrounded `next dev` escapes the worktree](reference-background-dev-server-escapes-worktree.md) — it writes .next into the MAIN repo and 500s every request with ENOENT on routes-manifest.json; the absolute path in the error is the only tell
- [An awk range collapses to ONE line when start also matches end](reference-awk-range-collapses-to-one-line.md) — a committed-red harness assertion can look unsatisfiable; run its awk by hand, then fix the CODE SHAPE, never the harness
