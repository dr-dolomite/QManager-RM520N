# ui-builder memory index

- [Signal chip = identity, not quality](feedback-signal-chip-identity-not-quality.md) — NR blue / LTE violet fill, quality moved to the Material glyph's bar count; metric value tints stay green/amber/red for both radios
- [Approved off-ramp font sizes on the dashboard](feedback-signal-card-13px-off-ramp.md) — 13px signal rows, 44/52/26px speedtest numerals, 11/17px latency tile; the impeccable hook flags them every edit, keep them, don't add a suppression
- [Material subset: grep the array, then REGENERATE](project-material-subset-gaps.md) — 109 glyphs; `icons:subset` reaches Google Fonts from here, so adding one is completable in-session (binary font diff), not a blocked two-part change
- [No prettier in this repo](reference-no-prettier-in-repo.md) — `bunx prettier --check` downloads a stock-default prettier that fails on untouched files too; gate is `eslint` + `tsc --noEmit`
- [`t` in a polling hook's deps kills the interval](reference-t-identity-churn-kills-intervals.md) — no `bindI18n` override, so a language switch churns `t`, rebuilds the callback, re-runs the effect and stops polling forever; use a `tRef`, never an eslint-disable
- [Locale parity baseline is 100%](reference-locale-parity-baseline-is-100.md) — any en-only key addition = N×4 strict errors; delete retired keys from all five packs; locale JSON is CRLF so the round-trip guard must normalise or it always aborts
- [Throwaway `app/qm-preview/` fixture route](reference-visual-verification-fixture-route.md) — hash-driven state matrix + `@width` maxWidth for container queries; the 3 traps: `&`-backgrounded dev server looks dead but isn't, a wedged Chrome tab mimics an infinite render loop, and deleting the fixture breaks `tsc` until `.next` is cleared
- [Standing explanatory copy gets cut](feedback-explanatory-copy-gets-cut.md) — cost paragraphs, pre-action hints and duplicated counts were all removed 2026-08-14 despite documented rationales; prefer conditional, state-scoped copy over permanent prose
- [Middot survives only in machine-voice runs](feedback-middot-only-in-machine-voice-runs.md) — `·` as generic glue was called out; label the value and let layout separate. Watch for shared keys whose call sites pre-prefix a param
- [Tag/Badge-shaped skeletons need `bg-accent` put back](reference-tag-skeleton-bg-transparent.md) — `tagVariants()` carries `bg-transparent` so the placeholder renders invisible; `badgeVariants()` carries the role fill so it pulses as a finished chip. Nothing lints it
- [Custom `rounded-*` overrides win or lose ALPHABETICALLY](reference-custom-radius-overrides-lose-alphabetically.md) — VERIFIED in built CSS: `.rounded-field` is emitted before `.rounded-md`, so a Skeleton renders 6px not 20px; the fix is a trailing `!`
- [An awk range collapses to ONE line when start also matches end](reference-awk-range-collapses-to-one-line.md) — durable part: run the awk by hand before believing a claim about code shape
- [Draft state must be DERIVED, not synced by effect](reference-draft-must-be-derived-not-synced.md) — the `useEffect` copy of server values into a draft is an ESLint ERROR here and silently eats typed edits on every re-read; hold `Partial<Draft>` edits and overlay them
- [A held row's dim goes on TEXT, never ROOT](reference-row-dim-compounds-with-disabled-opacity.md) — every disabled control ships `disabled:opacity-50` and opacity compounds, so a 0.6 row renders its field at 0.30
- [A brief's "nothing reads this" is a claim, not a premise](feedback-verify-zero-consumer-claims.md) — `ok` on RealtimeDataPoint was declared unread; the sibling ping-entries-card.tsx reads it to print "Timeout"
- [Reaching /dashboard on the dev server](reference-reaching-dashboard-on-the-dev-server.md) — `navigate` upgrades localhost to https and kills the tab; use `preview_start` with the url, set `qm_logged_in=1`, and prove a cascade settled with computed opacity, never a screenshot
- [A hidden Browser pane runs NO frames — rAF and CSS animations both freeze](reference-hidden-browser-pane-freezes-raf.md) — `document.hidden` still says false; makes SaveButton overlap and pins `animate-in` at opacity 0, and doubles as a free Non-Load-Bearing Rule test
- [recharts 2.15.4: range areas are native; `tooltipType="none"` hides them](reference-recharts-range-area-and-tooltip-none.md) — a `[low,high]` tuple dataKey draws a band with no stacked pair; `labelFormatter` gets the config LABEL, not a numeric x value
- [A family refit's shell is updated LAST, so your route 500s on a sibling](reference-family-refit-shell-blocks-verification.md) — keep an alias export when you rename; never read a sibling's compile error as your own bug
- [A frozen pane pins CSS TRANSITIONS at t=0, not just rAF](reference-frozen-pane-freezes-css-transitions-too.md) — a transitioned box-shadow reads back all-zero while its own `--tw-shadow` is correct; inject `*{transition:none!important}` before measuring
- [Next dev exposes `window.next.router` — soft-nav away and back to REMOUNT a page](reference-soft-nav-remount-keeps-fetch-shim.md) — works on a qm-preview FIXTURE only; on a real route the segment cache survives, so trigger the surface's own refresh() instead
- [Measuring contrast here: lab() colours and a class-only theme flip](reference-measuring-contrast-in-this-repo.md) — `getComputedStyle().color` is `lab()` so a regex parser returns ~1.00 for everything; convert via canvas, and switch themes with localStorage+reload, never a classList flip
- [A class split across a template boundary is NEVER generated](reference-tailwind-scanner-needs-literal-bang.md) — Tailwind scans literals only, so the `!` must live inside the const; alerts/shapes.ts ships the unsafe form and its 42px field is inert
- [A parallel session already owns this worktree's dev server AND the browser pane](reference-parallel-sessions-share-browser-and-devserver.md) — the `.next/dev/lock` error names the cause; match the port by process cmdline, and pass `tabId` on every browser call or your keystrokes land in a sibling's tab
- [`Badge variant="info"` IS `bg-primary-container` — it dissolves on a promoted row](reference-info-chip-dissolves-on-primary-container.md) — same two tokens as Highlight-by-Container; re-ground at `/20`, which measures 1.41:1, parity with a stock chip on a card
- [Eyebrow casing is CONTENT here, not CSS](reference-eyebrow-casing-lives-in-content.md) — no `EYEBROW` class applies `uppercase`, so caps in a JSON leaf ships to every locale; one outlier split `it` (mirrored caps) from `zh-CN` (normalised)
- [Locale packs are STATIC imports — a fetch shim cannot inject a key](reference-locales-are-statically-bundled.md) — an unwritten key renders its own dotted path, which is the sharpest branch evidence there is; force backend state on a real route by shimming fetch then clicking the surface's own Refresh
- [A stretched card grid hides a skeleton delta](reference-stretched-grid-hides-skeleton-delta.md) — `items-stretch` + `*:h-full` makes both cards in a row report the row max; measure `card-content`, and note a `flex-1` skeleton group can be 50px short and still look right
- [Locale numstat proof is the DELETIONS column](reference-locale-numstat-proof-is-the-deletions-column.md) — on an uncommitted worktree the whole subtree reads as added, so a big additions count is not a CRLF blow-up
- [`satisfies Record<K,V>` LOSES an optional field on union access](reference-satisfies-breaks-optional-tone-fields.md) — it keeps each entry's literal type, so members that omitted `spin?` really lack it; use the `: Record<K,V>` annotation, which enforces the same thing
