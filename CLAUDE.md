## How to Use This File

This file is loaded into **every** session — keep it lean. Everything here is a **golden rule to follow**: the Communication Style, Design Context, and platform/backend truths below are non-negotiable and always apply.

Detailed feature and subsystem notes live in `docs/reference/`. **Do NOT read those reference docs preemptively** — open one only when the current task actually touches that subsystem. Reading them "just in case" wastes context. The same applies to `PRODUCT.md` and `DESIGN.md` — read them when doing product or UI work, not for backend fixes.

## Communication Style

When reporting findings, diagnoses, root causes, or explaining how something works, write so the user **learns alongside the fix** — not just expert-to-expert shorthand.

- **Lead with a plain-English summary** (one line) before the technical specifics. Example: "Short version: the CGI script can't see `jq` because lighttpd starts CGI scripts with a stripped-down `PATH` that doesn't include `/opt/bin`."
- **Briefly explain the *why*** behind the underlying mechanism — one or two sentences of context. Example: "lighttpd does this on purpose: untrusted CGI scripts shouldn't inherit the parent shell's environment, so it gives them a minimal one."
- **Define jargon on first use**: acronyms (CGI, RLS, RSRP, EN-DC), kernel/system terms (sysctl, udev, systemd target, journald), protocol terms (flock, PTY, WebSocket upgrade) get a one-clause gloss.
- **Use analogies** when they clarify ("`flock` is like a 'do not disturb' sign on the file — only one process can hold it at a time").
- **Keep it additive, not bloating.** Trivial answers ("yes", "the file is at X") don't need a tutorial. The rule kicks in for findings, diagnoses, post-mortems, code review, and architecture explanations.

This applies to all output that explains *what's happening* or *why* — bug investigations, debug session reports, audit findings, design rationale, and any "I traced this and found..." moments. **Exception:** `RELEASE_NOTES.md` copy targets end users — see Release Notes below; brevity wins there.

## Change Workflow

Every code-change request in this repo follows a tier-routed, 6-phase flow. The main session orchestrates; the specialist agents do the work. The user holds the approval gate. This flow is the project default for code changes and supersedes the generic brainstorming / writing-plans / verification skills; test-driven development still applies inside Phase 4 wherever tests exist.

**Signal each phase transition** with a header so the user always knows where we are: `**[Phase 1 — Triage]**`, `**[Phase 2 — Plan]**`, `**[Phase 3 — Approval]**`, `**[Phase 4 — Execute]**`, `**[Phase 5 — Validation]**`, `**[Phase 6 — Docs & Close]**`.

### The 6 Phases

1. **Triage & Recon (orchestrator):** Classify the request into Tier 0–4 by blast radius. For every **bug fix**, every **Tier 3+** change, and **all Tier 4** work, dispatch `modem-investigator` as a read-only Phase 1 gate — it maps the UI→hook→CGI→`qcmd`→modem flow statically and probes live state via Posh-SSH before any code is written. It returns an evidence report (file paths with line numbers, captured CGI/systemd/journal/log output, findings), never code. If the change touches the installer, systemd units, sudoers, `/usrdata/` layout, or the OTA pipeline, also dispatch `installer-safety-auditor` as a hard read-only gate. Synthesize findings.
2. **Plan (orchestrator synthesizes, builders pre-flight):** For Tier 2+, dispatch builder agents in parallel — `cgi-endpoint-builder` (backend CGI / daemons / libs / AT flows) and/or `ui-builder` (pages / cards / hooks / types). They return scaffolding + design notes, NOT committed code. Synthesize into ONE plan: tier, agent roster, file list, build order, risks, post-flight validator list.
3. **Approval Gate (user):** Plan changes here are cheap; later changes are not.
4. **Execute (builders):** Bottom-up for cross-layer work: poller → CGI → hook → component → alerts. Parallel where files are independent; sequential where there's a data dependency.
5. **Post-Flight Validation (parallel, ONE message):** Fire every applicable validator in a single message: `busybox-portability-checker` (static audit **and** scoped on-device verification of the deployed change), `installer-safety-auditor` (verify mode, for installer/systemd/OTA changes). Loop failures back to Phase 4 — but after **2 failed validation rounds**, stop and surface to the user instead of looping further.
6. **Docs & Close (`docs-writer`):** Update `docs/reference/`, the routing tables in this file, and `RELEASE_NOTES.md` as needed. Report summary + git status.

### Tier Routing

| Tier | Scope | Flow |
|------|-------|------|
| 0 | Typos, comments, copy edits, version bumps | Direct edit, no agents, no plan |
| 1 | Single existing file in one layer | Skip Phase 2–3. Implement + the layer's validator + maybe docs |
| 2 | New feature, single layer | Full flow; pre-flight is the layer's builder only |
| 3 | Cross-layer feature (CGI + hook + component, or a poller field consumed across layers) | Full flow; `modem-investigator` runs the Phase 1 recon gate |
| 4 | Installer / systemd / sudoers / `/usrdata/` layout / OTA pipeline | Full flow; `modem-investigator` recon **plus** `installer-safety-auditor` as a hard Phase 1 gate before code is written |

Bug fixes match the tier of the *fix*, not the bug — and always get a Phase 1 `modem-investigator` recon first, because "understand the live flow before touching it" is cheaper than a wrong fix. Pure refactors with no behavior change drop one tier (validators still run; builders don't pre-flight).

### Agent Roster

All agents are defined in `.claude/agents/`. Models are pinned per agent — the orchestrator does not choose them.

- **Recon gate (Phase 1, read-only):** `modem-investigator` — traces the full stack statically and probes the live modem read-only via Posh-SSH; returns an evidence report and can halt work before code is written.
- **Safety gate (Phase 1 + Phase 5, read-only):** `installer-safety-auditor` — audits installer/systemd/sudoers/`/usrdata/`/OTA changes; can BLOCK before code is written; re-runs in verify mode post-change.
- **Builders (Phases 2 & 4):** `cgi-endpoint-builder` (backend CGI shell endpoints, AT/`qcmd` flows, daemons, apply pipelines), `ui-builder` (Next.js / shadcn / Tailwind frontend).
- **Validator (Phase 5):** `busybox-portability-checker` — static audit (shebang, CRLF, BusyBox applet limits, 32-bit arithmetic) **and** scoped on-device verification of the deployed change.
- **Closer (Phase 6):** `docs-writer`.

### Hard Rules

- **Tier is decided once, up-front.** If tempted to skip the recon or a validator mid-flow, re-triage rather than skip.
- **`modem-investigator` is read-only and fails loud.** If recon reveals the change needs a write action on live state, or surfaces a broken invariant, it halts and reports — the main thread re-routes through the builders + validators.
- **The Phase 1 `installer-safety-auditor` gate fails loud.** BLOCKED halts the work before code is written. This is cheap; rework is not.
- **Post-flight validators always go out in a single parallel message.** Never serially.
- **Validate CGI as `www-data`, never as root.** On-device CGI checks go through lighttpd (`curl http://127.0.0.1/cgi-bin/...`) or `sudo -u www-data` — root-shell testing with `_SKIP_AUTH=1` has masked real permission bugs before.
- **No in-flight reboot.** The app runs on the modem itself — `reboot` / `AT+CFUN=1,1` mid-request kills the in-flight HTTP response and the device. Reboots are deferred (dialog + persistent banner after the response is written); validators reject inline reboots in a CGI response path.
- **`docs-writer` is the closing bracket.** If it doesn't run on Tier 2+, the change isn't done.
- **Any new UI string or nav item is i18n-keyed, across all 5 locales.** Sidebar/nav sub-items use `t_key` (never a raw `title`), and every new key is added to all of `public/locales/{en,zh-CN,zh-TW,it,id}/*.json`; `bun run i18n:check` must pass (100% parity gate) before a frontend change is done. A builder branched before the i18n conversion will reach for `title` — convert it.
- **Installer changes move in lockstep across install + uninstall + OTA.** A feature that adds a binary, service, or config must touch `install_rm520n.sh`, `uninstall_rm520n.sh`, and the update/OTA path together — a service the uninstaller doesn't remove, or a config the updater doesn't preserve/seed, is an incomplete change. This is exactly what the `installer-safety-auditor` gate verifies.
- **Agents don't see the orchestrator's conversation.** Each dispatch is a self-contained brief with file paths, schemas, the live evidence from `modem-investigator`, and the relevant `CLAUDE.md` / `DESIGN.md` / `PRODUCT.md` sections inlined.

### Branch Model

- **`development` is the true integration base.** It is the branch the user works on day-to-day and the branch **all feature/worktree work must be based on and merged back into**. When in doubt about "the originating branch," it is `development`.
- **`main` is the stable-release branch.** A feature branch merges to `main` **only when the user explicitly decides** a version is stable and tested enough to release / keep for future reference. Never merge to `main` on your own initiative — it is a deliberate release act the user gates.
- **⚠️ `EnterWorktree` bases the new branch on `origin/<default-branch>` = `origin/main`, which LAGS `development`.** This has already bitten a run: a worktree branched from `main` (v0.1.12) while `development` was 44 commits ahead (v0.1.31), and 6 diverged files had to be re-applied onto `development` at close-out. **So immediately after `EnterWorktree`, verify the base:** `git merge-base HEAD development` must equal `git rev-parse HEAD`. If it doesn't, the worktree is on a stale base — re-create the branch off `development` (or re-apply onto a `development`-based branch) **before any builder writes a file**, never after.

### Worktree Discipline (Tier 2+)

Parallel branches and parallel builders must never cross-contaminate commits. Two layers of isolation, both harness-native:

1. **Run-level — every Tier 2+ run gets its own worktree, based on `development` (see Branch Model — verify the base is not stale `main`).** Immediately after the Phase 3 approval gate (before any builder writes a file), call `EnterWorktree` to create an isolated checkout on a fresh branch named for the change (e.g. `wt/eth-link-alerts`). The session CWD moves there and every subsequently spawned teammate inherits it. Phases 1–3 (recon/plan) stay in the main checkout — they're read-only and should see the branch the user actually asked about. Tier 0/1 edits stay in-place, no worktree.
2. **Agent-level — isolate builders only when file sets overlap.** If two builders would touch overlapping or uncertain file sets in parallel, spawn them with `isolation: "worktree"` and reconcile their results into the run worktree. When file sets are provably disjoint (the normal case — backend in `scripts/`, UI in `components/`), skip it; they share the run worktree.

**On entry, fix the three things a fresh worktree is missing:**
- **`.env` is gitignored** → copy it from the main checkout or `modem-investigator` / `busybox-portability-checker` silently lose SSH access to the live modem. Verify `git check-ignore .env` still holds in the worktree; never commit it.
- **`node_modules` is absent** → run `bun install` lazily, only if the change actually needs a frontend build/lint/tsc pass; backend-only changes skip it.
- **`/reimagine/` is gitignored** (`.gitignore:71`) → the design-mock bundle (`Recommended Hybrid`, the `Motion Guide`, the index deck) does not exist in any worktree. Same failure class as the `.env` gap, and quieter: a `ui-builder` briefed to "match the mock" finds nothing there and improvises without saying so. Copy the directory in from the main checkout before briefing any agent that must read a mock, or inline the relevant mock excerpt into the brief; never commit it.

**Close-out (Phase 6):** after validation passes and `docs-writer` closes, ask the user via `AskUserQuestion` — merge back into the originating branch, keep the branch for a PR, or discard — then `ExitWorktree`. Never auto-merge.

**After merging into a fast-moving branch, run the full build (`bun run package`, or at minimum `next build` + `bun run i18n:check`) — the pre-merge typecheck is not enough.** A clean auto-merge can still break the build: files that each pass in isolation can collectively violate a contract that advanced on the target branch (this session: a builder's `{ title }` nav item met a merged i18n change that now required `t_key`, and only the post-merge `next build` caught it). Resolve conflicts by integrating both sides — for shared index/notes files (`CLAUDE.md`, `RELEASE_NOTES.md`, `docs/reference/README.md`, the installer's gated-service list) keep the target branch's entries and graft the feature's rows in, never clobber.

### Skip Phrases

User can short-circuit by saying "just do it" / "skip the plan" / "tier 0 it" — drop to direct execution. Otherwise the flow is the default.

### Orchestration Mode ("orchestrate")

When the user says **"orchestrate"** (e.g. "orchestrate this", "orchestrate a team for…"), run the 6-phase flow above as a **multi-agent team**, not a solo pass. Tiers, gates, and the user approval gate all still apply. Default shape:

- **The orchestrator is the head architect, not a worker.** It plans, briefs teammates, synthesizes their evidence, holds the approval gate, and makes the calls. The legwork (recon, builds, validation, docs) goes to teammates. The orchestrator still does its own targeted reads to *judge* teammate reports rather than rubber-stamp them.
- **Teammates are spawned liberally and in parallel**, each with a **self-contained brief** (file paths, schemas, live evidence, the relevant CLAUDE.md/DESIGN.md/PRODUCT.md sections inlined) — they don't see the orchestrator's conversation. Use the project agents (`modem-investigator`, `cgi-endpoint-builder`, `ui-builder`, `busybox-portability-checker`, `installer-safety-auditor`, `docs-writer`) plus `Explore`/`general-purpose` for recon.
- **One teammate is always a dedicated devil's advocate** for any investigation — its job is to attack the leading hypotheses, surface what the team is underweighting, and stop the team from "fixing" accurate telemetry or chasing a phantom.
- **Phase 1 recon fans out.** Run several read-only agents at once on different leads (live `modem-investigator` probing, static `Explore`, a delta/compare angle, the devil's advocate). When new evidence lands mid-flight, **redirect a running teammate with `SendMessage`** instead of re-spawning. If a backgrounded teammate goes idle without delivering its report, ping it for the report.
- **Synthesize, then gate.** Fold all reports into ONE plan and use **`AskUserQuestion`** at the Phase 3 approval gate and for any real scoping decision. Don't start Phase 4 builders until the user approves.
- **Worktree Discipline applies (see above).**
- **Execute → validate → docs, with a task board.** Builders run bottom-up (parallel where files are independent), validators gate every backend/shell change on-device, `docs-writer` closes. Track it with `TaskCreate`/`TaskUpdate` (owners + blockers) so the user can follow progress.
- **UI craft stays with the orchestrator** via the Impeccable skill; `ui-builder`/`Explore` may still recon the surfaces.

The same Skip Phrases still apply — "just do it" drops orchestration back to a solo direct pass.

## Design Context

See **`PRODUCT.md`** (strategic: what QManager is, users, brand personality, aesthetic references/anti-references, design principles) and **`DESIGN.md`** (visual: OKLCH tokens, typography, status-badge pattern, layout rules, component conventions, motion, Do's and Don'ts). Read them before any UI or product-facing work.

**⚠️ A design-language redirect is in flight.** `DESIGN.md` now carries a Material-3-inspired tonal system derived from the new QManager mark (filled `*-container` roles, filled status chips, a 28/36/40px shape scale, pill controls, 300-400ms emphasized motion, Material Symbols Rounded scoped to the **sidebar + the `/dashboard` route + the two pre-auth routes `/` and `/login/`**, lucide on every other route — the **Icon-Boundary Rule**, which replaced the retired Nav-Glyph Boundary Rule; it is keyed on ROUTES not directories, and `/setup/` is deliberately still lucide; see `docs/reference/icon-system.md`). It is the **committed target and the binding canon for new design decisions**. **Migration step 1 has landed:** every token resolves in `app/globals.css`, so `bg-primary-container`, `bg-surface-container`, `rounded-pill`/`rounded-card`, and `ease-emphasized` are live. Two roles ship under non-canon names to avoid colliding with shadcn neutrals: Carrier Violet is `--lte-*`, Uplink Teal is `--uplink-*` (see `DESIGN.md` > Token Names in Code). The legacy `rounded-{sm,md,lg,xl}` chain keeps its old values; new work uses the role radii (`rounded-inline/field/tile/card/hero/pill`). Components are **not** retargeted yet: steps 2 to 4 in `DESIGN.md` > Migration Sequence still stand. The quick reminders below describe **what ships today**; flip the badge table here to the filled-chip pattern in the same change as migration step 3, not before.

Quick reminders the visual spec enforces (current shipped state):

### Status Chip Pattern
All status indicators are **filled tonal chips**: a `Badge` variant carrying a role container fill, that container's `on-` ink, no visible border, pill radius, and a `size-3` icon (lucide, or a `MaterialSymbol` with an explicit `size` on the dashboard route). The variant is the whole API — never hand-write the classes, and never use `variant="outline"` for a status indicator (that is the retired Outline-Badge Rule).

| State | Variant | Renders | Icon |
| ----- | ------- | ------- | ---- |
| Success/Active | `success` | `bg-success-container text-on-success-container` | `CheckCircle2Icon` |
| Warning | `warning` | `bg-warning-container text-on-warning-container` | `TriangleAlertIcon` |
| Destructive/Error | `destructive` | `bg-destructive-container text-on-destructive-container` | `XCircleIcon` or `AlertCircleIcon` |
| Info | `info` | `bg-primary-container text-on-primary-container` | Context-specific (`DownloadIcon`, `ClockIcon`, etc.) |
| Muted/Disabled | `muted` | `bg-surface-container-high text-on-surface-variant` | `MinusCircleIcon` |

```tsx
<Badge variant="success">
  <CheckCircle2Icon className="size-3" />
  Active
</Badge>
```

- `components/ui/badge.tsx` is the shared wrapper: the five roles above live in its `cva`, so a status chip is correct by construction rather than by reviewer discipline. `default` / `secondary` / `outline` remain for non-status labels (network type, category tags, counts)
- **Every status chip carries an icon.** `success-container` and `warning-container` measure **1.03:1** apart — the same surface to the eye, and identical under deuteranopia — so the glyph is the only thing separating a healthy state from a degraded one. Two states in the same slot must never share a glyph either
- Tone maps key onto the exported `BadgeVariant` type, never onto a class string, so a new tone without a matching role fails the build (`REBOOT_TONE_BADGE`, `TONE_BADGE`, `qualityBadgeVariants`, `getQualityBadgeVariant`)
- Choose muted for deliberately inactive states (Stopped, Offline peer, Disabled); destructive for failure/error states (Disconnected link, Failed email)
- The opacity washes (`bg-{role}/5`, `/10`, `/15` on icon discs, tiles, pulse rings and inline notices) are a **separate, still-unmigrated** family. They are not chips; do not flip them as part of chip work
- **`nr` / `lte` are IDENTITY variants, not status roles** — they say which radio a chip belongs to (blue `primary-container` / violet `lte-container`) and never mean "healthy". The five roles above stay the only correct choice for a status indicator. Where a chip's fill carries identity, the quality it also reports must be encoded non-chromatically (the dashboard signal cards use the Material glyph's bar count). See DESIGN.md > Identity-Chip Rule

### UI Component Conventions
- **CardHeader**: Always plain `CardTitle` + `CardDescription` without icons. Icons belong in badges or separate action areas, not in the card header itself.
- **Primary action buttons**: Default variant (not outline) for main actions like Record, Save, Apply. Use `SaveButton` for save-specific actions with loading animation.
- **Step-based progress**: `Loader2Icon` spinner + dot indicators for step/sample progress. Reserve fill/progress bars for data visualization (signal strength, quality meters) only.
- **Typography**: Euclid Circular B is the UI typeface (`--font-sans`); Geist Mono (`--font-geist-mono` → `font-mono`) is scoped to machine-voice surfaces per DESIGN.md's Machine-Voice Rule. No other typeface is loaded. Both light and dark mode are first-class (OKLCH tokens); radius 0.65rem base.
- **Components**: use shadcn/ui primitives before hand-rolling; semantic color tokens only, never raw Tailwind colors.

## RM520N-GL Platform

QManager targets the Quectel RM520N-GL modem, which runs **vanilla Linux internally** (SDXLEMUR SoC, ARMv7l, kernel 5.4.210) — NOT OpenWRT on an external host. The app (Next.js static export + CGI shell backend) is deployed **onto the modem itself** and is fully standalone. Because the app runs on the device, anything that reboots the modem also kills any in-flight HTTP request — defer reboots via dialog + persistent banner, never `AT+CFUN=1,1` mid-request.

### Live Device Access

A live RM520N-GL is reachable over SSH — **probe it whenever you can verify an architecture claim or assumption directly instead of guessing.** Credentials are in `.env` (`MODEM_IP`, `MODEM_SSH_USER`, `MODEM_SSH_PASSWORD`) — gitignored, local-only. Connect with the POSH-SSH PowerShell module (`New-SSHSession` / `Invoke-SSHCommand`). The device is the source of truth for platform facts; docs drift.

Typical read-only probes: `systemctl status <unit>` / `journalctl -u <unit> -n 50`, `/tmp/qmanager_*.json` runtime state, `/etc/qmanager/` + `/usrdata/` config files, `curl -sS http://127.0.0.1/cgi-bin/quecmanager/...` (CGI through lighttpd), `qcmd 'AT+...'` query commands, `pgrep -fa qmanager`, `iptables -t mangle -L -n`, `/proc/net/dev`.

**Safety:** treat the modem as a live system — no reboots, `AT+CFUN=1,1`, factory resets, service restarts, or config writes without a stated reason. Never echo `.env` values into transcripts; reference the variable names. Deep investigation belongs to `modem-investigator`; scoped post-deploy checks to `busybox-portability-checker`.

### System Differences

The table below contrasts RM520N-GL against the legacy RM551E (OpenWRT) target — useful when porting or reading older code.

| Concern | RM551E (OpenWRT) | RM520N-GL (Vanilla Linux) |
|---------|-----------------|---------------------------|
| Init system | procd | systemd (`.service` units in `/lib/systemd/system/`) |
| Config store | UCI | Files in `/usrdata/` (persistent partition) |
| Root filesystem | Read-write | UBIFS — read-only by default on stock boot (`mount -o remount,rw /`) |
| Shell | BusyBox sh (POSIX only) | `/bin/bash` available |
| Web server | uhttpd | lighttpd (Entware) |
| Firewall | nftables / fw4 | iptables direct |
| TTL interface | `wwan0` | `rmnet+` |
| Package manager | opkg (system) | Entware opkg at `/opt` (dedicated UBIFS volume) |
| LAN config | UCI (`network.*`) | `/etc/data/mobileap_cfg.xml` via xmlstarlet |

### Reference Docs

Read these only when working on the relevant subsystem:

- **AT command transport** (`atcli_smd11`, `qcmd`, SMS, flock serialization) — `docs/reference/at-command-transport.md`
- **QManager standalone install & runtime internals** (Entware bootstrap, udev permissions, CGI auth, service persistence, firewall, Tailscale, web console, email/SMS alerts, OTA pipeline incl. opt-in auto-update timer gated on `update.auto_update_enabled` — armed at install/OTA AND live by the Software Update UI toggle via the `qmanager_auto_update_arm` root helper) — `docs/reference/qmanager-independence.md`
- **Full platform architecture** (platform internals, Entware bootstrapping, lighttpd config, boot sequences, troubleshooting) — `docs/rm520n-gl-architecture.md`

**Source reference:** `simpleadmin-source/` contains the original RM520N-GL admin panel (iamromulan/quectel-rgmii-toolkit) for historical reference. QManager is now fully independent and does not require SimpleAdmin to be installed.

## Release Notes (`RELEASE_NOTES.md`)

Fixed template — the file's normal end-state is a **single active release entry** with all of these elements:

1. `# 🚀 QManager RM520N BETA vX.X.X` heading
2. **One-line summary paragraph** (rewritten each release to hook on the headline change)
3. OTA blockquote, verbatim: `> One-click OTA from **System Settings → Software Update** if you're on v0.1.5 or newer.` (the v0.1.5 anchor is fixed)
4. `## ✨ New Features` / `## 🛠️ Improvements` / `## 🐛 Fixes` (any subset)
5. `## 📥 Installation` with `### Upgrading from vX.X.X` (only the version number rotates) and `### Fresh Install` (curl + wget blocks verbatim)
6. `## 💙 Thank You!` — GitHub Issues link, support links, and the `**License:** MIT + Commons Clause` line, all verbatim

**Tone per bullet:** bold plain-English lead → one short sentence of user-visible behavior (say where in the UI) → optional compressed technical parenthetical for power users. ~1–2 sentences per entry; 3 only for a migration note. No post-mortem paragraphs — that register belongs in `docs/`, not release notes.

## Removed/Deferred Features (dev-rm520 Branch)

The following features have been **completely removed** from the `dev-rm520` branch. Their backend scripts, frontend components, hooks, and types no longer exist. Do NOT reference, modify, or create code for these features unless explicitly re-porting them.

| Feature | Reason | Scope of Removal |
|---------|--------|-----------------|
| VPN Management (NetBird only) | Third-party binary, fw4/mwan3 dependencies | CGI, hooks, components for NetBird |
| Video Optimizer / Traffic Masquerade (DPI) | nftables dependency, nfqws ARM32 not validated | CGI, hooks, components, types, dpi_helper.sh, installer |
| Low Power Mode | Daemons removed earlier; `save_low_power` CGI action + `low_power_*` config seeds retired in the crond→systemd-timer migration — no Low Power code remains | qmanager_low_power, qmanager_low_power_check, `save_low_power`, `low_power_*` seeds |

## Feature-Specific Notes

Detailed operational notes for individual features live in `docs/reference/`. Read the relevant file only when working on that feature:

- **Icon System / Icon-Boundary Rule** (Material Symbols on the sidebar + `/dashboard` + the pre-auth `/` and `/login/` routes, lucide elsewhere — the rule is ROUTE-scoped, so `components/auth/change-password-dialog.tsx` correctly keeps lucide (it mounts from the authed sidebar) and `/setup/`'s `components/onboarding/**` is deliberately still lucide, meaning mounting the converted `LoginLanguagePicker`/`ModeToggle` there would silently walk the boundary; subset is 64 glyphs / 23.9 KB and now loads on the FIRST page a visitor sees; the self-hosted subset pipeline `bun run icons:subset`, the single-source `MATERIAL_SYMBOL_NAMES` list + `bun run icons:check` manifest gate, the inline-`fontSize` sizing gotcha, the two Network Status exceptions) — `docs/reference/icon-system.md`
- **Auth Rate Limiting** (progressive login lockout ladder in `scripts/usr/lib/qmanager/cgi_auth.sh`: 5 free attempts then 30/120/300/900s, escalating on each SINGLE subsequent failure, 1h decay, cleared by a successful login; state in tmpfs `/tmp/qmanager_auth_attempts.json` + `.lock`, all fields jq-defaulted so a pre-ladder file loads; `qm_get_rate_limit_status` is READ-ONLY and is the ONLY limiter call `auth/check.sh` may make, because `app/page.tsx` hits it on every public-splash load; mutations run in `( … ) 9<lock` subshells so callers must re-read state after; counter is global-not-per-IP and reboot-clearable, both deliberate; `attempts_remaining: 0` on an UNLOCKED form is a real state meaning "next wrong password re-locks"; `scripts/test/auth-lockout-ladder.sh`) — `docs/reference/auth-rate-limiting.md`
- **Antenna Alignment** (`/cellular/antenna-alignment`) — `docs/reference/antenna-alignment.md`
- **Carrier Aggregation** (`AT+QCAINFO` → `parse_ca_info()` → `network.carrier_components[]`; LTE bandwidth is resource blocks and NR is a QENG enum, both decoded to `bandwidth_mhz` — `lte.bandwidth` is a raw enum and is NOT MHz; `earfcn` carries the NR-ARFCN for NR components; NSA reports exactly one PCC held by LTE so the frontend derives an `"nr-anchor"` role; any failed AT read resets `carrier_components` to `[]`, which is why the dashboard strip debounces releases client-side; view model in `lib/carrier-aggregation.ts`, widget `components/dashboard/carrier-aggregation.tsx` replaces the deleted `scc-status.tsx`) — `docs/reference/carrier-aggregation.md`
- **Recent Activities** (dashboard event feed; `events.sh` NDJSON at `/tmp/qmanager_events.json` **cap `MAX_EVENTS=300`** — five producers append and each trims, so `qmanager_poller`/`qmanager_watchcat`/`qmanager_profile_apply`/`profile_mgr.sh`/`profiles/deactivate.sh` must ALL agree or the smallest wins → `at_cmd/fetch_events.sh` serves only the newest `EVENTS_SERVE_LIMIT=50` via `serve_ndjson_as_array`'s OPTIONAL 2nd arg (default 0 = whole file, so `fetch_ping_history`/`fetch_signal_history` are unaffected) → `use-recent-activities` 10s poll keeping 20 newest-first, i.e. **300 on disk / 50 served / 20 kept / 5 drawn**; PRODUCER-SIDE band+PCI are settle-debounced by `_ev_settle` (`events.sh:242-249`): a value must hold `EV_SETTLE_SAMPLES=3` consecutive NON-BLANK observations (~9s — measured cadence is ~3s, `POLL_INTERVAL=2` is a `sleep` AFTER the body, not a period) across 4 channels `lte_band`/`lte_pci`/`nr_band`/`nr_pci`, state in `eval`-built poller globals `_ev_{hold,holdn,last,flapn,flapt,flapr}_<ch>` NOT persisted (matches the latency/loss streak precedent, so `restore_event_state`'s positional `cut -f` decode is untouched); a BLANK is an absent observation not a value — `parse_serving_cell` blanks all four on any unusable `AT+QENG` line (i.e. every service drop) and `snapshot_event_state` now preserves last-known-good (`prev_ev_lte_band="${lte_band:-$prev_ev_lte_band}"` ×4), which also stops the "5G NR anchor lost (was …)" clause going empty; wording is **"LTE band settled on B28 (was B41)"** NOT "changed from…to…" because the radio really does walk the intermediate rungs and a debounced event may only state endpoints, never a transition; rc 2 = churn — `EV_FLAP_THRESHOLD=6` candidate changes inside `EV_FLAP_WINDOW=300`s emits ONE **`warning`**-severity row ("LTE band unstable — 14 changes in the last 5 min"), the first time `band_change`/`pci_change` have carried `warning`, absorbed with ZERO frontend change because `glyphOf` is tone-first (amber + `TriangleAlertIcon`, not the routine handoff glyph) and `{event.message}` is rendered raw so no locale keys; the rate limit is its OWN latch `_ev_flapr_<ch>` — resetting only the counter let a period-2 oscillation re-trip every ~18s and rebuild the storm at warning severity; the three constants are HARD-CODED not config keys because `settings/quality_thresholds.sh:103-114` rebuilds that JSON from scratch with exactly two keys AND `install_backend` (`install_rm520n.sh:1287-1298`) only deploys config files that don't already exist, so a third key would be erased on the next preset save and never reach an upgraded device; fixtures in `scripts/test/poller-phase-bcd.sh` replay the live capture and the oscillation case; pure `lib/event-presentation.ts` does the one derivation the backend does not: `computeUnresolved()` pairs each degradation with the recovery that cancels it in ONE forward pass (cross-type `RESOLVED_BY`, same-type-at-`info` `SELF_RESOLVING`, everything else a one-shot that can never be unresolved, though it CAN still be tonal while fresh; a `type|severity` seen-later set makes three stacked "Internet Lost" rows light exactly one) and MUST be fed the full 20, never the sliced 5, or a recovery past the clip edge stops resolving; DESIGN.md's **Age-Gated Tone Rule** splits two axes: TONE (`EventTone` = `error`/`warning`/`success`/`routine`, severity-first then `RECOVERY_TYPES` **or** `SELF_RESOLVING` — the latter is what makes a GAINED carrier green: `ca_change` carries both directions in one type, the severity guards run first so an info-severity self-resolver can only be the recovery half, so "LTE CA activated"/"carriers changed 2→3 (+B3)" and "5G NR anchor acquired" are success while CA deactivated / count-shrink stay warning; never expires, carried by a FILLED ICON DISC in the solid role colour — `DISC_FILL` = `bg-{role} text-{role}-foreground`, the one slot that never consults the age gate, so an aged warning row goes grey and keeps a full-strength amber disc) vs. WEIGHT (age), so `presentEvent(event, unresolved, fresh)` draws the tonal container when `fresh || unresolved` (OR, not AND: `isFresh` = `FRESH_WINDOW_SEC` 3600s handles the common case, `unresolved` is the safety valve so age retires history but never a still-standing problem) and otherwise settles to `bg-surface-container` keeping the disc; `routine` fill is deliberately ACHROMATIC `surface-container-high` because `severity:"info"` means routine not good (band change `:649`/`:697`, PCC handoff `:661`/`:708`) and `primary-container` is dark-mode L 0.400 vs 0.300/0.320/0.325 for success/warning/destructive, i.e. a handoff would outshine an outage; aged-vs-fresh is only 1.16-1.24:1 (dark `success`/`destructive-container` ~1.00:1, hue-only) so glyph + `sr-only` severity word before the message are mandatory, and two states in one slot must never share a glyph; `error` IS emitted, only by `qmanager_watchcat` (`:468`/`:483`/`:512`/`:528`/`:596`)/`qmanager_profile_apply` (`:702`) and only on one-shot types, but `destructive-container` is now REACHABLE via freshness: an error one-shot is filled red for its first hour then settles to a red disc on the plain surface; **row anatomy is Motion Guide recipe 04, NOT `Recommended Hybrid`** — 28px disc + `size-4` glyph, message as the primary `text-sm` line, `font-mono text-xs` timestamp caption under it, NO event-type label (the message and disc already say it; the `activities.events.*` keys stay parity-clean but unrendered); `routine` has no solid role so `routineDisc(tonal)` steps the disc one surface DOWN on a fresh row and one UP on a settled one (down twice would land on `surface` and erase it); `metaClass` is `opacity-90` not the mock's 75%, which measures ~3.5:1 and fails the floor; the message needs `truncate` or a wrapped line breaks `ROW_H`; arrival is `x:"100%"` (a full entrance from the trailing edge — the shipped `x:24` was 6% travel and read as a twitch), contained by the list's both-axis `overflow-hidden`; `VISIBLE_ROWS = 5` is the single source for `LIST_MAX_H`/`RENDER_COUNT` **and** `ActivitiesSkeleton` (a hard-coded 6 there would jump the page 68px at the skeleton handoff); `useNowSec()` ticks 30s independent of the fetch because the hook's error path never calls `setEvents`, so a sustained failure would otherwise freeze freshness under a still-rendering stale list, and `useTimeAgo` takes that ONE `nowSec` (plus a `Math.max(0,…)` clamp) so label and gate can never straddle the hour; chip hidden while loading/erroring (Saved-State Honesty), `error` split into no-data alert vs. notice-above-stale-list, key is `timestamp-type-message` because `pci_change` fires from four sites in one tick; `constants/network-events.ts` `EVENT_LABELS` no longer read by this card but kept — `network-events-card.tsx` and `watchdog-recovery-activity-card.tsx` are live call sites — and `formatTimeAgo` left alone for the same reason) — `docs/reference/recent-activities.md`
- **Dashboard chart cards** (Device Metrics / Live Latency / Signal History; recipe-16 chart draw-in via `.chart-draw` CSS over recharts — a live chart runs TWO clocks, split by `hooks/use-chart-motion.ts`: the ENTRANCE is CSS and must be applied via `useChartDrawIn()` (never a hardcoded `chart-draw` class — recharts remounts each series on every data change, so a permanent class replays the draw every poll), the poll UPDATE is recharts' own interpolation configured by `{...useChartSeriesMotion()}` (300ms/`--ease-standard`, reduced motion handled in the hook since `MotionConfig` cannot reach react-smooth); `pathLength={1}` still required on every series; series colours are `--chart-nr`/`--chart-lte`, never the numbered `--chart-1..6`; `MetricBar` tone classes must stay static maps) — `docs/reference/dashboard-chart-cards.md`
- **Dashboard state-change motion** (the two gestures that fire when the modem reports something new, both now in shared primitives; **live value tick cascade** — `components/ui/tick-group.tsx` `TickGroup` renders NO DOM and is one-per-card-body, `useValueTick` ENQUEUES inside a group instead of starting, group drains on a shared MICROTASK (not a parent layout effect — a memoized subtree updating alone would strand its registrations) and starts each member at `rank × TICK_STAGGER_STEP` **100ms** — a THIRD stagger step and the product's only NON-ENTRANCE one, deliberately not either entrance step: an entrance cascade races the user's patience so it stays dense, a tick cascade races nothing (the card is already on screen) and only has to read as a sequence, so it shipped wrong at the 40ms row step where 4 dips spanned 120ms against a much longer dip, overlapped, and still read as one flash; floor ~80ms (dips merge below it), ceiling is the poll, and the mock demo's 350ms is still NOT the value (2s looping showcase's legibility spacing; at 350ms a 5-value cascade spends 1.4s on lead alone and outlives its own poll); DESIGN.md's "only two stagger steps" rule now reads "only two ENTRANCE steps" and that exception is CLOSED; the tick itself was raised one scale step to `DUR.standard + DUR.emphasized` = **700ms**, dip at ~43%, down leg on `EASE_STANDARD_CSS` but the RETURN leg deliberately **`"linear"`**, breaking the each-leg-carries-its-own-token's-curve rule on measured evidence: the eased return went 0.35→0.89 in its first 100ms then spent 300ms crawling 0.89→1.00 (invisible on text), so a nominally-700ms gesture was perceptually ~400ms — **a duration is only as long as its visible part**, and EVERY curve on this scale is front-loaded because they are shaped for content that ARRIVES, whereas a return-to-rest has no arrival to sell and needs the only ramp with no invisible tail; measured live by pulling a running tick's keyframes off the page and stepping a probe through fixed `currentTime` values, which is clock-INDEPENDENT and therefore the only method that works while the automated tab is backgrounded (Chrome freezes the animation clock in a hidden tab, so sampling `getComputedStyle` mid-tick reads `1.00` everywhere and looks like a broken animation); rank is over the values that CHANGED not ordinal position — Device Information's 9 never-changing identity rows would otherwise dip at 540/600ms and Device Metrics would cascade with holes; order comes from live DOM nodes via `compareDocumentPosition` so document order IS reading order at every breakpoint (CA's `grid-cols-1→2→4` places row-major) AND a conditionally-rendered value is ABSENT rather than a dead beat (Signal Status' `rows[0]` band row mounts no tick); `MAX_RANK=7` clamp; the cascade FORCED the retarget fix in `hooks/use-value-tick.ts` — read computed opacity BEFORE `cancel()` (cancelling discards it) + `fill:"backwards"` or a delayed tick is snap→freeze→dip, same construction as `.ca-meter`'s `animation-fill-mode: backwards`; registration is `useLayoutEffect` not `useEffect` or the drain runs first; CA now carries TWO cascades — `--meter-index` fill arrival at 60ms on first paint vs. `TickGroup` value dip at 100ms every poll; a `TickGroup` between a variant parent and its `motion` children does NOT sever `staggerRows`→`staggerRowItem` (motion resolves via `useContext(MotionContext)`, not adjacency); **status chip morph** — container half in `badge.tsx` was already correct, the LABEL half is now the shared `components/ui/swap-label.tsx` `SwapLabel`, never a hand-rolled keyed `motion.span` (no `AnimatePresence` = half a crossfade), glyph goes INSIDE the swap (it is the only greyscale channel between `success`/`warning` at ~1.03:1) but the `sr-only` name stays OUTSIDE (`mode="popLayout"` doubles it for 180ms), key must encode text AND variant where both move; `network-status.tsx` is the reference implementation) — `docs/reference/dashboard-state-motion.md`
- **Custom DNS** (`/local-network/custom-dns`, dnsmasq upstream override via sentinel block in `/etc/data/dnsmasq.conf`) — `docs/reference/custom-dns.md`
- **Data Usage Counter** (kernel `/proc/net/dev`-sourced, schema v5 with static SoC-based orientation map from `/etc/quectel-project-version`, `modem_reset_count`, `orientation`) — `docs/reference/data-usage-counter.md`
- **Ethernet Status & Link Speed** (`/local-network/ethernet`, Realtek RTL8125B 2.5GbE on `eth0` via `r8125` driver; reads link state from sysfs, speed/duplex from `ethtool`; speed limit applied via `qmanager_ethernet_apply` root helper; lib at `scripts/usr/lib/qmanager/ethtool_helper.sh`; `qmanager-ethernet.service` uses a `[Unit]`-placed `ConditionPathExists` so an idle device reports inactive, not `failed`)
- **Centralized Alerts** (`/monitoring/alerts`, one page + one CGI `monitoring/alerts.sh` + one engine `alert_engine.sh` for SMS/email/Discord; 3 events × 3 channels routing×capability matrix in `/etc/qmanager/alert_routing.json`; boot-id reboot ledger; the three legacy pages `email-alerts`/`sms-alerts`/`discord-bot` are now redirects to `/monitoring/alerts`) — `docs/reference/alerts.md`
- **Discord Bot** (`discord-bot/`, deployed as `/usr/bin/qmanager_discord`; now a pure DM transport driven by `alert_engine.sh` via `/tmp/qmanager_discord_cmd`) — `docs/reference/discord-bot.md`
- **WAN Profile Management** (`cellular/apn.sh`, 6 PDP contexts, AT-only, per-context `AT+CGACT` cycle; APN Settings page renders a pixel-strict RM551E single-APN card backed by the additive `apn_setting.json` sidecar + `active`/`active_cid`/`cids[]` GET fields and `save`(no-`index`)/`deactivate` POST actions — 6-slot backend retained, unreached from the page) — `docs/reference/wan-profile-management.md`
- **Custom SIM Profiles** (4-step apply `apn → ttl_hl → scenario → imei`; top-level `scenario:{default,schedule}` object mirrored to `settings.scenario_id` at a single `profile_save` chokepoint, up to 2 daily windows applied via a runtime-generated systemd `OnCalendar` timer — RM520N has no crond — armed by root helper `qmanager_scenario_schedule_arm`; ICCID compares canonicalized via `iccid_canonicalize` (strips BCD pad); `--auto` apply mode guards against a SIM switch superseding an in-flight apply, pending-apply queue is latest-wins; active profile gates APN / TTL/HL / Scenarios / Band Locking pages; `profile_managed` CGI guard; UI is the RM551E-parity redesign — 4-tab create/edit wizard (Identity→Network→Scenario→Review), stacked-card saved-profiles list (`custom-profile-table.tsx` removed), hero-glyph apply dialog rendering the 4 RM520N steps; Verizon UX omitted (RM520N-only), `vzw` stays an ordinary MNO preset; scenario picker "+ Create new" deep-links `?action=create`; **the profile schema carries NO band fields** — band locks are reachable ONLY via a bound `custom-*` scenario's `config.lte_bands`/`nsa_nr_bands`/`sa_nr_bands` (colon-joined bare decimals, no `N` prefix), which is why a save must create the scenario FIRST; `qmanager_profile_apply::_spawn_band_failover_if_needed` spawns `qmanager_band_failover` on the `custom-*` apply path for parity with `bands/lock.sh` — the profile route previously had no revert net while binding a `custom-*` scenario disables the manual Band Locking page (same `band_failover_enabled` flag/PID/`www-data` backgrounded-subshell spawn, no sudo); create form autofills on mount via `useCurrentSettings(true)` — render-time prefill (not `useEffect`) with an `explicitLoad` flag: MOUNT = fill-empty-only + NEVER IMEI (apply step 4 `AT+EGMR=1,7` reboots), EXPLICIT "Load from SIM" = overwrite incl. IMEI, `cid`/`pdp_type` ride with `apn_name` as one triple; `current_settings.sh` gained additive `spn`/`network_name`/`mcc`/`mnc` via `;+QSPN` on the SAME compound read (MCC/MNC split first-3/rest, fails soft to `""`; `spn` is QSPN's **3rd** quoted field = `EF_SPN` = the reseller's own brand, `network_name` is the **1st** = `EF_PNN` = the network — `$2` was originally mislabelled `spn`, invisible on the GLOBE test SIM where all three names match; the PLMN guard clears ONLY the numeric fields so a malformed PLMN can't blank the names); **suggested profiles** "Recommended for your SIM" (`constants/profile-suggestions.ts` + pure `lib/carrier-match.ts` + `hooks/use-profile-suggestions.ts` + `SuggestionRow` **inside** `custom-profile-view.tsx` — suggestions render as ordinary-looking rows appended to the Saved Profiles list under a divider, but stay a SIBLING PROP, never merged into `profiles[]`: the count badge, the `Promise.all(profiles.map(getProfile))` detail prefetch, and the activate/edit/delete wiring all depend on that separation; the empty-state gate is widened to `profiles.length === 0 && suggestions.length === 0` so a zero-profile user still sees the recommendation) — 8 recipes across `PLMN_TABLE` (T-Mobile ×2, Verizon, AT&T, Smart, Globe, GOMO, DITO), all TTL/HL 64, **only the T-Mobile pair carries bands** (no verified recommendation elsewhere, and a band lock forces a `custom-*` scenario that disables Band Locking); detection = PLMN gate THEN `MVNO_SPN_DENYLIST` refinement matched EXACT-normalized (never substring — `"mobile"` is inside `"tmobile"`) against `spn`+`network_name`, denylist can only REMOVE a match never create one because `EF_SPN` is optional; Verizon's primary PLMN is **311**-480 not 310, which is why `MCC_US` alone stopped working; shared-PLMN pairs (`ambiguous_plan`) T-Mobile/TMHI 310-260 and Globe/GOMO 515-02 render the "which plan?" warning, others don't; visibility DERIVED (carrier match AND no saved profile matching the live ICCID) with **no persisted dismissal flag** — `config.sh` has no key-migration primitive, bands intersected with `supported_{nsa,sa}_nr5g_bands`; create makes a scenario ONLY when `hasBandLock && scenario_name` else binds built-in `"balanced"` (an empty intersection previously still minted a `custom-*` scenario locking nothing — strictly worse than the built-in), rollback deletes the scenario only if we created it; poller `network.carrier` (COPS) deliberately NOT used — names the tower, same MVNO ambiguity + free-text drift; `constants/mno-presets.ts` deliberately untouched) — `docs/reference/sim-profiles.md`
- **SIM Detection** (TWO stores, don't conflate: the known-SIMs **set** decides *"is this SIM new"* — `sim_db.sh`, `/etc/qmanager/known_iccids`, **line format FROZEN** (`grep -qxF` whole-line match; adding any field makes every known SIM look new — all metadata goes in the sidecar), migrated once from the retired `last_iccid`; the **SIM registry** sidecar decides *"should the banner still show"* — `/etc/qmanager/sim_registry.json` root:root 0644 keyed by the SAME `sim_db_normalize` ICCID, `{carrier, phone_number, first_seen, dismissed}`, lib `sim_registry.sh` (scoped-field atomic writes, BusyBox-safe `flock` poll loop — no `-w`), root helper `qmanager_sim_registry_apply` (bare-path sudoers, ALL validation inside, rc 3 = unknown ICCID, never auto-creates), CGI `system/sim_registry.sh` GET/list + POST dismiss/undismiss; **`known_sims.sh`'s `clear` spans BOTH stores** — `sim_db_clear_keep` + `sim_registry_clear_keep` via the helper's `clear_keep` action (the one sanctioned whole-object registry rewrite, kept record preserved VERBATIM so an existing dismissal survives; `clear_keep` is also the ONLY action accepting an empty ICCID, gates keyed on `[ -n "$iccid" ]` not on the action), returns additive `registry_cleared` so the UI warns instead of over-promising when the sidecar write fails — clearing one store only was a shipped bug (count 2→1, list still 2); poller derives `status.json.sim_swap.detected` from record-exists AND `first_seen` non-empty AND `!dismissed` (shape unchanged; `matching_profile_*` still from the `/tmp` flag), installer `migrate_sim_registry()` backfills `known_iccids` as `dismissed:true`/`first_seen:null`; dead `dismiss_sim_swap` watchdog action + the localStorage dismissal store both REMOVED; registry is NOT tamper-proof vs. www-data (`/etc/qmanager` is www-data-owned); UI = `sim-registry-card.tsx` "Tracked SIMs" + redesigned `sim-swap-banner.tsx`; admin CGI `system/known_sims.sh` count/list/clear behind `hooks/use-known-sims.ts`, rendered as that card's `CardFooter` (count + info tooltip + destructive Clear) — `known-sims-row.tsx` and its mount inside the System Settings card are REMOVED; colocation is load-bearing, not cosmetic: Clear acts on the SET while the card lists the REGISTRY, so one owner guarantees the post-clear `refresh()` that stops the two halves drifting) — `docs/reference/sim-detection.md`
- **Connection Watchdog** (`/monitoring/watchdog`, `qmanager_watchcat` state machine reading `qmanager_ping.json`; split-ownership — Watchdog owns fail cadence `watchcat.fail_threshold` (compared vs. raw `streak_fail`, no double-debounce) + probe cadence `watchcat.probe_interval` (sole writer → propagated to `ping_profile.json.interval_sec`), the Connection Quality "Probe Targets" card owns only `target_ipv4`/`target_ipv6`; the producer's `limited`/tri-state short-circuit is now INERT (ICMP producer never emits it — dead code, ladder unaffected, still keys off `streak_fail`); `max_failures`→`fail_threshold` migrated in installer + defensive CGI runtime, `qm_config_delete` primitive; 4-tier ladder T1 COPS re-register / T2 CFUN toggle / T3 SIM failover / T4 deferred reboot with token bucket; Tier-3 Golden Rule swap with `SIM_SETTLE_SECS=90` floor + `verify_quimslot` read-back gate (`slot_verify.sh`, shared with the manual SIM-slot switch CGI) before trusting a landed slot, persistent `/etc/qmanager/qmanager_sim_failover` + `known_iccids` set registration on finalize/revert, misconfig stops the ladder; two-pass CGI validation) — `docs/reference/connection-watchdog.md`
- **Connection Quality** (`/system-settings/connection-quality`, the measurement/telemetry side — sibling of the Watchdog; `qmanager_ping` is now a `#!/bin/sh` **ICMP** `ping` producer (ported from RM551E for parity; the Rust `ping-daemon/` crate is retired-but-present, pending deletion) — IPv4-first (`target_ipv4`, default `1.1.1.1`) then IPv6-fallback (`target_ipv6`, default `2606:4700:4700::1111`), slim `/tmp/qmanager_ping.json` schema `reachable`/`streak_fail`/`last_family`(ipv4\|ipv6\|none); **tri-state `connected`/`limited`/`disconnected` REMOVED** — deliberate tradeoff losing carrier-intercept/captive-portal detection, and ICMP reachability is per-carrier variable (a carrier that filters ICMP → false "disconnected"); `migrate_ping_targets()` OTA migration reseeds ICMP keys + deletes legacy `target_1`/`target_2`; `qmanager_poller` UNCHANGED (null-safe jq degrades), derives latency/jitter/loss/history into `status.json.connectivity`; Probe Targets card owns `profile`+`target_ipv4`/`target_ipv6`, Latency & Loss Thresholds card owns alert presets feeding `events.sh` `high_latency`/`high_packet_loss` (NOT recovery); `ping_profile.json` two-writer atomic key-merge — see connection-watchdog.md for cadence ownership) — `docs/reference/connection-quality.md`
- **Timezone / System Clock** (`/system-settings`, glibc reads `/etc/localtime`; applied by copying a TZif via the `qmanager_timezone_apply` root helper; zoneinfo at `/opt/share/zoneinfo` from Entware `zoneinfo-all`; `/etc/TZ` is inert on glibc; cron adopts a new zone on the next reboot) — `docs/reference/timezone.md`
- **Scheduled Reboot & Tower Lock Schedule** (System Settings → Scheduled Operations, `/cellular/tower-locking` → Schedule) — RM520N ships a `crond` binary but **never runs it** (no unit, no boot symlink, empty `/var/spool/cron/crontabs/`; `which crond` lies), so the old cron-line writes were silent no-ops. Both now use runtime-generated systemd `OnCalendar` timers via shared `scripts/usr/lib/qmanager/schedule_timer.sh`, armed by root helpers `qmanager_scheduled_reboot_arm` (single `qmanager-scheduled-reboot.timer`) and `qmanager_tower_schedule_arm` (atomic `apply`+`clear` timer pair), mirroring `qmanager_scenario_schedule_arm`; `Persistent=false` (no stack-fire after downtime); `/lib/.../timers.target.wants/` manual symlink, NOT `systemctl enable`; charset-gated `_qm_validate_hhmm`/`_qm_validate_days` (newline-injection hardening) before any value hits `OnCalendar=`; reboot worker has a `/tmp/qmanager_update.pid` OTA guard; save responses carry `armed:true|false` so the UI warns on an unarmed schedule; installer re-arms config-driven on every OTA, `qmanager_update` `scrub_legacy_cron()` strips dead markers. Band Locking resolves the in-force scenario (incl. active schedule window) via `lib/scenario-schedule.ts::resolveScheduledScenario` — `docs/reference/scheduled-timers.md`
- **Overview Splash + `/login/` (the two pre-auth surfaces)** (public unauthenticated landing at `/`; `app/page.tsx` 3-state gate `public`/`checking`/`redirecting` — confirms the optimistic `qm_logged_in=1` cookie via `auth/check.sh` before forwarding to `/dashboard/`, else falls through to the splash so a stale cookie can't create a login trap; `OverviewCard` polls 3 public GET-only CGI endpoints under `scripts/www/cgi-bin/quecmanager/public/` — `overview.sh` is a strict jq allowlist projection of `/tmp/qmanager_status.json` with ZERO AT/`qcmd`, `hostname.sh` reads the KERNEL hostname from `/proc` (diverges from `settings.hostname`), `units.sh` reads temp/distance prefs without writing; hooks use plain `fetch` `credentials:"omit"` NOT `authFetch`; deliberate `logout()` → `/`, involuntary/credential-change redirects stay at `/login/`; **both surfaces are now retargeted to the M3 tonal system against `Login and Overview.dc.html` and move together** — one 48px pill CTA role across two consecutive screens, so `DESIGN.md`'s pre-auth type-scale exception (19/17/15/13/11px, mirrored in `.impeccable/design.json` as `pre-auth-card`) governs both; `overview-card.tsx` split into `components/public/overview/{tone,tiles,band-rows,states}` with the seven-branch state chain extracted to a pure `resolveBodyMode()` so the footer caption and the body can never disagree; NEW shared primitive `components/ui/tonal-banner.tsx` (card-scoped sibling of the page-level `banner.tsx`, 3 tones, Material glyphs, Solid-Container + Glyph-Disc rules) carries login's session-lost/rate-limit banners and the overview's stale banner; rate-limit banner is WARNING not destructive and modem-unreachable is WARNING with a "signing in still works" footer; band chips are IDENTITY per the Identity-Chip Rule (NR `primary-container`, LTE `lte-container` — NOT `secondary-container`, the comp's `--sc` ships here as `--lte-*`); NO shake on wrong password (Motion Guide bans rubber-band); `check.sh` gained additive `rate_limited`/`retry_after`/`attempts_remaining` and `useLogin()` returns `lockout` so a reload mid-lockout restores the countdown — see auth-rate-limiting.md) — `docs/reference/overview-splash.md`
- **i18n / Language Picker** (`react-i18next`; 5 core langs `en`/`zh-CN`/`zh-TW`/`it`/`id` **bundled** — compiled in via `lib/i18n/resources.ts`; client-only-lazy init is a hard invariant for the static export; native `_one`/`_other` plurals — **no** `i18next-icu`/`http-backend`/`languagedetector`; `localStorage["qmanager_lang"]`; `bun run i18n:check` key-parity gate. Increment 1 = `common`/`sidebar`/`dashboard` namespaces. **Contributor pipeline (Increment A):** off-device `bun run lang` CLI (`scripts-dev/lang.ts`: scaffold/status/check/fmt/build/publish) + shared `lib/i18n/pack.ts` validation engine + i18n-parity CI. **Runtime downloader (Increment B):** installs *additional* community packs onto a live modem WITHOUT firmware update — CGI under `scripts/www/cgi-bin/quecmanager/system/language-packs/` + double-forked worker `qmanager_language_install` + shared lib `language_packs.sh`; persistent store `/usrdata/qmanager/locales-packs/<code>/` re-copied into served `www/locales-packs/` by `install_frontend` after the OTA wipe (NOT symlink, NO systemd unit); privileged commit via root helper `qmanager_language_pack_apply` (one bare-path sudoers line); trust root pinned to the project's own GitHub `language-packs` release (SSRF gate); sha256 verified before extract; BusyBox-`tar` hardening rejects symlink/hardlink/`..`/non-allow-listed members; kernel `flock` fd 9 concurrency (409 if busy); runtime `addResourceBundle` injection via `lib/i18n/runtime-pack.ts` + cold-boot fallback fix in `config.ts`. the language-pack **manager** UI copy itself is still English-only, no `t()`. Namespaces now shipped in all 5 locales: `common`, `sidebar`, `dashboard`, `cellular`, `system-settings`) — `docs/reference/i18n.md`, `docs/CONTRIBUTING-translations.md`
- **SMS Center** (`/cellular/sms`, `cellular/sms.sh` via bundled patched hard-float `sms_tool` on `/dev/smd11` under the shared `/tmp/qmanager_at.lock`; CPMS **ME+SM** dual-storage routing — reads/tags/merges both memories and self-heals `AT+CPMS="ME","ME","ME"` so incoming SMS never strand on the SIM, re-asserted at boot by the `qmanager_sms_storage` oneshot; read/unread is **client-side** — `sms_tool -j` drops the flag and `CMGL=4` marks all read — via `hooks/use-sms-read-state.ts` djb2 fingerprint + `localStorage["qmanager.sms.read.v1"]`; `YYMMDD` timestamp reorder for newest-first; the `sms_tool` binary is deployed by `install_bundled_binaries()` — unconditional, runs even under OTA's `--skip-packages`, so the patched smd11-default binary is refreshed on every upgrade; UI copy is fully i18n'd via the `cellular` namespace `sms.*` subtree across all 5 locales) — `docs/reference/sms.md`
- **SMS Forwarding** (`/cellular/sms/forwarding`, `cellular/sms_forwarding.sh` + daemon `qmanager_sms_forward`, the **only** server-side inbox reader; 15s poll, **seed-on-first-run** so it never sprays the existing inbox, `From <sender>:` loop guard, 3×/5s retry with registration recheck, failures cap 20; per-invocation shared-lock; config `/etc/qmanager/sms_forwarding.json` lazy-created + reload flag; `qmanager-sms-forward.service` `Type=simple` in `UCI_GATED_SERVICES` so OTA preserves on/off; CGI toggles via `svc_enable`/`svc_restart` never raw systemctl; lifted-hook two-card UI) — `docs/reference/sms-forwarding.md`

If you add a substantial new feature with non-obvious invariants, drop its notes into `docs/reference/<feature>.md` and add a row above rather than re-fattening this file.

## Shared Constants

- **`ANTENNA_PORTS`** (`types/modem-status.ts`): Canonical metadata for 4 antenna ports (Main/PRX, Diversity/DRX, MIMO 3/RX2, MIMO 4/RX3). Used by `antenna-statistics` and `antenna-alignment`. Any new per-antenna UI must import from here — do not duplicate port definitions.
